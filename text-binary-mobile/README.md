# text-binary-mobile

移动端优先的文字与二进制互转网页应用，风格为莫兰迪色系扁平化 UI。

## 本地预览

直接双击 `index.html` 即可打开，或在仓库根目录执行：

```powershell
python -m http.server 8080
```

然后访问：

- `http://localhost:8080/text-binary-mobile/`

## 功能

- 文字转二进制（UTF-8）
- 二进制转文字（支持忽略空格和换行）
- 输入校验与错误提示
- 一键复制、清空、互换输入输出
- 移动端安全区适配
