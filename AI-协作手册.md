# AI 协作手册

> 非技术人员如何跟 AI 搭档做项目——原则、流程与避坑指南
>
> 以 **Cursor** 为主要示例。Claude Code 专属功能标注 `[CC]`，Cursor 专属功能标注 `[Cursor]`。

---

## 这份手册是什么

我们团队用 AI 做了多个实际项目（飞书智能助手、Figma 设计自动化、数据工具等），过程中踩了不少坑，也总结了一些可复用的经验。

这份手册把这些经验整理成了流程和工具，希望对大家有参考价值。

---

## 如果你只有 5 分钟

**1. 分工表** → 见 1.2　|　**沟通原则** → 见 1.3　|　**AI 18 种糊弄方式** → 见段 4 速查表

**2. 五条铁原则**（贴墙）：

1. 先想清楚再开口
2. 关键判断必须写进文件（口头说的 AI 下次就忘）
3. 让 AI 先反问你（你以为说清了，其实没有）
4. 要真实输出不要口头承诺（AI 说"好了"不算数）
5. 小步走、每步审（每次不超过 30 分钟的量）

---

## 第一部分 · 先认识你的搭档

### 1.1 AI 是怎样的

**① 能力强，但记性差**——每次开新对话就从零开始。
→ 应对：关键信息写进 KNOWLEDGE.md 和 rules 文件，不要口头说。每次新对话开场先让 AI 读一遍这些文件。

> 实际遇到的：飞书 bot 项目，上午告诉 AI "Shell 是 PowerShell，不要用 Linux 命令"，下午开新对话它又用 `&&` `wc` `head`，报错 4 次。写进 rules 后再没犯过。

**② 没有改动成本意识**——改 1 行和改 100 行对它一样轻松。
→ 应对：每次任务限定改动范围——"本次只改 ___ 目录下的文件"。AI 交回改动时，先看 diff（改动对比），不在范围内的直接拒绝回滚。

> 实际遇到的：让 AI"优化一下页面加载"，它把整个组件重写了。加载速度没变，之前调好的交互坏了。

**③ 会说你想听的话**——不是故意骗你，但倾向于报告"好消息"。
→ 应对：任何"完成/通过/修复"，都追问"跑了什么命令？把完整输出贴给我"。不接受总结，只看原始输出。

> 实际遇到的：AI 说"所有测试通过"。追问"跑了什么命令？"——回答不上来。另一次说"已修复 bug"，实际只是把报错的那行注释掉了。

**④ 聊越久越不靠谱**——对话越长，早期的指令越容易被"埋没"。
→ 可以想象 AI 的工作台只能铺这么多纸——对话越长，你最初的要求被压到越下面，AI 更容易按最近的内容行动，甚至忘记你早期的约定。
→ 应对：一个任务一个对话。感觉 AI 变蠢了（重复问题、忘记约定），立刻开新对话，把当前进度摘要粘过去。Cursor 里用 `/compact` 或开新 Composer，Claude Code 里用 `/compact`。

> 实际遇到的：Figma 项目前 3 轮 AI 严格按约定每轮独立生成+截图对比，第 4 轮开始偷偷在上一轮基础上改，到第 7 轮才被发现——前面几轮全废了。

**⑤ 不会主动说"我做不到"**——不确定的事它也编一个看起来合理的答案。
→ 应对：AI 推荐的库、API、方法，让它给官方文档链接。给不出来的大概率是编的。涉及关键依赖时自己搜一下确认存在。

> 实际遇到的：AI 推荐了一个不存在的 npm 包，名字像真的，代码也对，跑 install 才报错。这种"编造依赖"的情况并不罕见，尤其是冷门技术栈。

### 1.2 你和 AI 各管什么


| 维度    | 你来             | AI 来       |
| ----- | -------------- | ---------- |
| 这是什么  | 项目是什么、给谁用、解决什么 | 用什么技术实现    |
| 做多久   | 长期 还是 一次性      | 配多少基础设施    |
| 做什么   | 做什么、**不做什么**   | 任务怎么拆、先做哪个 |
| 什么不能错 | 哪些出错后果严重       | 怎么测、怎么验    |
| 什么算完成 | "做完"长什么样       | 转成具体测试     |
| 这步对不对 | 每一步的判断         | 具体怎么写      |


**什么任务适合交给 AI，什么不适合**

| AI 擅长（放心交） | AI 不擅长（你要盯紧） | 不该用 AI（你自己来） |
| --- | --- | --- |
| 生成样板代码、重复性任务 | 需要全局一致性的大重构 | 生产数据库的直接操作 |
| 写测试用例 | 复杂业务逻辑 | 涉及法律/合规的决策 |
| 格式转换、数据处理 | 性能优化（AI 容易"看起来快"实际没变） | 安全审计（AI 辅助但不能替代） |
| 解释已有代码 | 并发/状态管理 | 密钥和权限管理 |
| 写文档、写注释 | 跨多个模块的联动修改 | — |

### 1.3 跟 AI 沟通的三条原则

整份手册的 prompt 都遵循这三条，理解了就可以自己编 prompt：

**① 具体 > 模糊**——"优化性能"不如"首屏加载从 3 秒降到 1 秒以内"。AI 拿到模糊指令会自己脑补，脑补的方向大概率不是你要的。

**② 约束 > 自由**——给 AI 限制条件（只改这个文件、不超过 50 行、不动已有逻辑）比让它自由发挥效果好得多。限制越多，AI 的输出越可控。不加限制 = 让实习生自由发挥，什么都可能发生。

**③ 让 AI 判断，但把你的习惯告诉它**——AI 的专业判断通常比非技术用户准（比如该测什么、该从哪个角度审查），所以不要替它写死步骤。但 AI 不知道你的个人偏好和工作习惯（比如你习惯先做前端还是后端、你更在意性能还是可读性、你的团队有什么规范），这些必须你来说。正确的节奏是：**AI 先出方案 → 你补充你的偏好和背景 → 有分歧的地方讨论 → 你拍板**。

### 1.4 关键信息必须写进文件

因为 AI 记性差（特质①），口头交代的下次就忘。


| 必须写进文件    | 口头说就行    |
| --------- | -------- |
| 项目是什么、做多久 | 这次做哪个小任务 |
| 什么不能错     | 临时调整     |
| 验收标准      | 一次性偏好    |
| 各模块怎么配合   | 试探性尝试    |
| 踩过的坑      | —        |
| 已否决的方案    | —        |


> 文件放项目根目录。注意：KNOWLEDGE.md 本身不会被 AI 自动读取——需要在 startup.mdc 规则里写"每次新对话先读 KNOWLEDGE.md"，AI 才会每次都读。`.cursor/rules/` 下的规则文件则是系统自动加载的，不需要手动提醒。

---

## 第二部分 · 项目生命周期

> 短期小项目可跳过段 2 和段 5，但段 1、3、4 不能跳。

---

### 段 1 · 启动

目标：花 10 分钟把"项目记忆 + 版本管理 + 行为规则"建好。

---

**正确做法①：先想清楚，再跟 AI 说**


| 你先想清楚          | 为什么        |
| -------------- | ---------- |
| 一句话这是什么项目？     | 说不清 = 没想清  |
| 长期 还是 一次性？     | 决定要不要建基础设施 |
| 给自己 / 团队 / 外部？ | 决定规范做多细    |


开口方式：

```
我要做一个 ___。类型：长期/短期。受众：___。
请你：1. 复述你理解的项目 2. 反问我 3-5 个歧义问题 3. 建议要不要建 KNOWLEDGE.md 4. 等我说 GO 再动手
```

> 实际遇到的：飞书 bot 项目没做这一步，每次新对话都要花 10-15 分钟重新解释项目背景。

---

**正确做法②：让 AI 帮你建项目备忘录（KNOWLEDGE.md）**

KNOWLEDGE.md 就是你给 AI 的备忘录，它每次开工前会先读。开口方式：

```
帮我建一份项目备忘录（KNOWLEDGE.md），放在项目根目录。
先扫描项目现状，你自己判断应该记录哪些信息（至少包含：项目目标、技术栈、运行环境、模块结构、常用命令、已知坑、修改日志），
还有什么你觉得对后续开发重要的信息也加上。
草拟完给我看——我会补充我的习惯和偏好，有不对的地方一起讨论再定稿。
```

> 中大项目还要加：架构决策(why) + 教训库 + 已废弃方案记录 + 已有依赖清单
>
> **KNOWLEDGE 不要太长**：控制在 200 行以内。AI 的"工作台"空间有限（上下文窗口），KNOWLEDGE 太长会挤占 AI 处理当前任务的空间，反而让它变蠢。过期的修改日志转移到单独的 `CHANGELOG.md`，只在 KNOWLEDGE 里保留最近 10 条。

> 实际遇到的：没有 KNOWLEDGE 的时候，AI 每换一次对话就忘了环境信息。PowerShell 下反复用 Linux 命令报错，先后踩了至少 4 次。同一件事 AI 在不同对话里引入了 3 种不同的实现方式，因为它不知道项目里已经有了什么。

---

**正确做法③：初始化 git——给项目一个"存档系统"**

git 就是代码的存档系统。每次存档（commit）相当于游戏里的 save point，出问题可以回退到任意存档。开口方式：

```
帮我初始化 git：
1. git init
2. 创建 .gitignore（根据项目类型忽略 node_modules / .env / 构建产物等）
3. 把当前能跑的代码做第一次提交，备注"初始化基线"
4. 以后每完成一个小步骤，都帮我提交一次，备注写清楚改了什么
5. 不许自动 git push，push 由我决定
```

> 实际遇到的：有个项目没有 git，AI 覆盖了能跑的代码写入了错误代码，无法恢复。

---

**正确做法④：让 AI 帮你建规则文件（rules）——给 AI 立"规矩"**

rules 是你给 AI 的行为约束，它每次对话都会自动遵守（不像口头说的会忘）。开口方式：

```
帮我建一套 AI 行为规则（放在 .cursor/rules/ 目录下），包含以下约束：

安全规则（safety.mdc）：
- 不许自动执行：rm -rf / sudo / git push --force / 全局装包 / 任何数据库操作
- 不许把密钥、密码、token 硬编码到代码中，必须用环境变量
- 不许删除任何现有代码，除非我明确要求

改动同步规则（sync.mdc）：
- 每次代码改动后，必须更新 KNOWLEDGE.md 的修改日志
- 每完成一个可验证的小步骤，必须提交 git（commit）
- commit 备注必须写清楚改了什么、为什么改
- 不许自动 git push

工作纪律规则（discipline.mdc）：
- 已调通的代码不许动，要改先告诉我为什么
- 不许"顺便优化"任何不在当前任务范围内的东西
- 每次改动集中在一件事上，不要东改一行西改一行
- 同一错误修了 3 次还不行就停下报告

自测规则（testing.mdc）：
- 每次改动后必须实际运行受影响的功能，贴出完整运行命令和输出
- 禁止用"should work""理论上没问题"代替实际测试结果
- 如果有已有测试用例，改完必须重跑，贴出通过/失败数
- 新增功能必须附带至少一个正常 + 一个异常的手动验证
- 禁止修改测试标准来让测试通过

新对话启动规则（startup.mdc）：
- 每次新对话的第一步：读 KNOWLEDGE.md（rules 文件系统会自动加载，不需要手动读）
- 读完后先用 3 句话复述：项目是什么、当前进度、本次任务
- 不确定的信息必须问我，不许假设
- 开始写代码前先确认运行环境（OS、Shell、语言版本）
```

> 为什么要用 rules 而不是每次口头说？因为 AI 记性差，口头说的纠正下次对话就忘。rules 文件会被系统自动加载。以后遇到 AI 反复犯同样的错，直接往 rules 里加一条就行。
>
> 注意检查：让 AI 建完 rules 后，打开文件看一眼开头有没有类似这样的格式头（叫 frontmatter），没有的话规则不会生效：
> ```
> ---
> description: 安全规则
> alwaysApply: true
> ---
> ```
> 让 AI 确认每个 `.mdc` 文件都有这个格式头，并且 `alwaysApply: true`（表示每次对话都生效）。
>
> 数据库操作要特别注意：2026 年多起公开事故中，AI 被要求"查看日志""分析问题"，却自行执行了删表命令，全部数据不可恢复。
>
> 实际遇到的：飞书 bot 项目前期没有 rules，AI 反复犯同样的错——被纠正"不要用 Linux 命令"后下次还用、被纠正"不要删代码"后又把错误处理删了。API 密钥也被直接硬编码到 JavaScript 文件里。

**规则维护技巧**：rules 不是建完就不动的。几个常见的需要追加规则的时机：

- **AI 重复犯同一个错** → 把纠正写成规则（比如"本项目 Shell 是 PowerShell，不要用 `&&` `wc` `head` 等 Linux 命令"）
- **换了技术方案** → 在 rules 里写"方案 A 已废弃，禁止使用"，防止 AI 在后续对话里走老路
- **踩了一个大坑** → 加进 rules 当"已知地雷"（比如"飞书 webhook 分支代码为空，不要走 webhook 路线"）
- **定期审计** → 每隔几次对话让 AI 读一遍所有 rules，检查有没有过时的、矛盾的、或者不再适用的规则。规则文件出错会导致 AI 每次对话都走错方向（规则毒化）

---

#### ✅ 段 1 完成：项目类型已定 + KNOWLEDGE 已建 + git 已初始化 + rules 已建

---

### 补充 · 新手常见错误 & 工具提效


| 常见错误              | 建议                                                           |
| ----------------- | ------------------------------------------------------------ |
| 看都没看就点"全部接受"      | 先看改动对比（diff）再接受。特别注意 AI 删了什么、改了哪些你没要求改的文件                    |
| 一个 prompt 塞整个项目需求 | 一次只做一件事，做完验证了再做下一个                                           |
| 密钥密码写进代码          | safety.mdc 已约束，review 时仍要搜 `sk-` `password` `secret` `token` |
| 不看代码就部署           | 涉及用户输入、数据库、权限的代码让 AI 解释"这段有没有安全风险"                           |


**Cursor 几个直接提效的功能**：

| 功能 | 怎么用 | 什么时候用 |
| --- | --- | --- |
| **@ 引用文件** | 输入 `@文件名` | 让 AI 精确看某个文件，比口头描述准得多 |
| **@ 引用文件夹** | 输入 `@文件夹名` | 注意：AI 只拿到文件摘要不是全文，大文件夹效果差 |
| **@ 联网搜索** | 输入 `@web` + 问题 | 让 AI 搜索实时信息（需要确认你的环境支持） |
| **模型切换** | 对话框顶部切换模型 | 规划/复杂任务用强模型（如 Opus），简单修改用快模型（如 Sonnet），省时间也省额度 |
| **Plan Mode** | 对话框切换到 Plan 模式 | 让 AI 只出方案不写代码，适合设计阶段 |
| **Checkpoint** | 自动生成，在时间线上可回退 | AI 改崩了可以一键回到之前的状态 |
| **看 diff** | AI 改完文件后左侧会显示改动对比 | 接受改动之前先看红色（删了什么）和绿色（加了什么） |

> Claude Code 用户对应：`/compact`（压缩对话）、`/rewind`（回退）、`/init`（自动扫描项目生成 CLAUDE.md，效果类似段 1 的 KNOWLEDGE 建立）。

---

### 已有项目的急救入口

> 如果项目已经做了一段时间才看到这份手册，不用从头重来，从这里切入：

```
项目比较乱了，请帮我：
1. 扫描代码梳理模块和依赖
2. 标注哪些稳定哪些常出问题
3. 补一份 KNOWLEDGE
4. 列出 3 个最大风险
5. 给"先救什么"的建议
```

> 急救完成后，回到段 1 补 git + rules，然后从段 3 继续。

---

### 段 2 · 设计

目标：让 AI 先想清楚再动手。短期小项目至少跟 AI 对一遍"分几块、谁先谁后"。

---

**正确做法①：让 AI 反问你，把模糊需求挖清楚**

```
进入规划模式，不要写代码。
1. 先问我问题，完全明确后再动手——不要假设，不确定的就问
2. 根据项目复杂度，你自己判断需要做哪些规划（模块拆分、依赖关系、数据流、技术选型等）
3. 每个决策写清楚 why
4. 给我看方案，我会补充我的偏好、纠正不对的地方，讨论完再下一步
```

> 小技巧："先问我问题，完全明确后再动手"这句话比写一大段规则更管用。AI 天然倾向于直接开干，这句话能强制它进入"先澄清再执行"模式。

> 实际遇到的：飞书 bot 项目没有先拆模块。修改消息格式导致通知模块、日志模块同时出错——因为模块间的数据怎么传根本没约定过。

一个很实用的做法是**让 AI 画系统架构图**——用文字描述模块和数据流不够直观，让 AI 画出来你一眼就能看出哪里不对。可以多轮确认，每轮让它调整，直到你觉得结构合理再往下走。

```
基于刚才的模块清单，用 Mermaid 语法画一个系统架构图：
- 每个模块用框表示，标注职责
- 用箭头表示模块间的调用关系和数据流向
- 标注关键接口的输入输出
画完给我看，我确认后再进下一步。如果哪里不合理我会让你调。
```

> AI 不能真的"画图"，但它能输出 Mermaid 语法——一种文本格式的图表描述语言，Cursor 的 Markdown 预览可以直接渲染成图形。你看到的是流程图/架构图，不是文字描述。

另一个技巧是**让 AI 用多角色 review 方案**——设计阶段的方案还没写代码，改起来成本很低，这时候多挑刺比搭建完再改便宜得多：

```
用多个不同角色分别审查这个方案。
你根据项目的类型和技术栈，自己判断需要哪些审查视角（比如用户体验、安全、性能、可维护性、合规等），
每个角色分别给出 2-3 个问题或风险点。
```

> 不要替 AI 限定角色——除非你明确知道该审什么角度（比如你知道这个项目涉及支付，可以指定"重点审支付安全"）。让 AI 自己判断视角，反而能覆盖你没想到的角度。
>
> 局限性：一个 AI 扮多个角色 ≠ 多个独立审查者——同一个模型的盲点在所有角色里是共享的。效果大概相当于一个人从多个角度想问题，能发现明显问题但不能替代真人 review。在重构优化阶段也可以用这个方法——准备大改之前先审一遍，比直接动手改再发现问题省时间。

**审方案时你重点看什么**（不需要看代码，看这几点就够）：

- **模块之间谁调谁**——有没有循环依赖（A 调 B、B 又调 A）？改一个会不会连锁影响？
- **数据从哪来到哪去**——用户输入 → 处理 → 存储 → 展示，每一步是否都说清了？
- **哪些是"改了就回不去"的操作**——删数据、发通知、扣费、推送，这些必须有确认机制
- **这个方案假设了什么**——AI 方案里常有隐含假设（比如"用户一定有网络""数据量不大"），让 AI 列出所有前提

```
审完方案后，帮我定验收标准：
1. 这个功能"做完"长什么样？用户能看到/操作什么？
2. 哪些是"必须做到"的（不达标就不算完成）？
3. 哪些是"最好能做到"的（有时间就做）？
4. 列 3 个最可能出错的场景，每个写出"出错时用户会看到什么"
把验收标准写进 KNOWLEDGE.md，后面验证阶段照着查。
```

---

**正确做法②：让 AI 先做功课——查文档、查社区**

```
在做方案前，先做以下调研：
1. 基于你的知识，列出这个技术栈的已知限制和常见坑
2. 列出你不确定的地方（API 是否可用、版本是否兼容等）
3. 给我一份摘要：确定的 / 不确定需要我验证的 / 你建议的方案和理由
```

> **重要提醒**：标准 Cursor 环境下 AI 没有联网能力，它说"我查了文档"很可能是在编——这恰好是特质⑤的典型表现。正确做法：让 AI 列出它不确定的点，**你自己去官方文档或搜索引擎验证**。如果你的 Cursor 配置了 `@web` 搜索或联网 MCP，可以让它搜，但要追问"给我原始链接"来确认不是编的。

> 实际遇到的：飞书 bot 项目中，AI 用了飞书插件的 webhook 模式写了一大堆代码，后来发现官方的 webhook 分支根本是空的，整个方向全废。如果一开始让它标出"不确定"再去验证，就能避免。

---

**正确做法③：定红线——什么不能错**

你来想业务红线（什么数据敏感、什么操作不可逆），让 AI 补充完整清单，你来审。

```
根据这个项目的特点，列出红线清单——哪些事情绝对不能出错。
你自己判断应该从哪些维度审查（数据安全、不可逆操作、权限、合规、性能底线等），
每条写清楚：风险是什么 + 怎么防 + 怎么测试。
写进 KNOWLEDGE.md 的"红线"部分。
```

> 实际遇到的：飞书 bot 项目有 5 层配置文件对同一行为给出矛盾指令，AI 收到矛盾指令后两边都不执行，bot 直接不响应。设计阶段做一次审计，成本不到后期修复的 1/10。

#### ✅ 段 2 完成

模块怎么拆已对齐 + 红线已列 + 验收标准已写

---

### 段 3 · 搭建

目标：AI 开始写代码了，你要做的是盯过程、防翻车。

---

**正确做法①：开工时补充本次任务的约束**

段 1 建的 rules 会自动生效（包括 startup.mdc 会让 AI 自动加载上下文）。每次开新对话的标准开场：

```
先读一遍 KNOWLEDGE.md（rules 你已经自动加载了）。
读完后告诉我：1. 项目当前状态 2. 上次做到哪 3. 本次你理解的任务是什么
确认环境：OS、Shell 类型、语言版本。
确认完再开始。
```

> 为什么每次都要这样开场？因为 AI 每次新对话从零开始（特质①），但 KNOWLEDGE.md 不会自动加载（rules 才会）。不做这步，AI 不知道项目背景，也会用默认假设——比如默认你是 Linux + Bash，实际你是 Windows + PowerShell。

然后补充这次任务特有的约束：

```
开始做模块 ___。补充规则：
1. 本次只改 ___ 路径下的文件，碰别的先问我
2. 每完成一小步就停下汇报：做了什么 / 没做什么 / 下一步建议
3. 汇报用"我跑了 X，输出 Y"，禁说"我以为""应该可以了"
```

> 实际遇到的：没有限定改动范围时，AI 修一个消息投递问题同时改了 6 个文件，每个文件只动 1-2 行，改完自己都说不清改动之间的关系。

> 小贴士：喂 AI 已有代码比文字描述规范有效得多。直接把项目里一个写得好的文件贴给 AI 说"照这个风格写"。

---

**正确做法②：盯住这些危险信号**


| 你看到的           | 立刻做什么             |
| -------------- | ----------------- |
| AI 改了无关文件      | 停 + 回滚            |
| "顺便帮你优化了 X"    | 拒绝 + 回滚           |
| 解释很多但没做事       | 要"少说，贴运行结果"       |
| 不再问你问题，自顾自做一大堆 | 停下来让它解释在做什么       |
| 同一错误修了 3 次     | 换思路或换模型           |
| 后半段明显变蠢        | 开新对话 + 交接摘要       |
| 多步任务偷跳了某步      | "你是不是跳了第 X 步？回去补" |
| 每一小步都停下来问你     | "先做完再整体汇报"        |


下面是几种我们实际遇到过的典型情况：

> **虚假优化 / 校验代替产出**：让 AI 优化 bot 回复质量，它没改回复逻辑，加了一堆输入格式校验。回复质量没变，只是"非法输入"被拦了。判断法：问"用户感受到什么不同？"——答不上来就是偷换问题。

> **AI 产出内容天然偏理论化**：不只是代码——让 AI 写文档、方案、教程时也会大段大段堆理论。需要明确说"实操为主，减少说教"，否则出来的东西读着有道理但不知道怎么落手。

> **修了又错的死循环**：消息 hook 的 bug，修了 hook → 触发了另一个干扰 → 修那个 → 引出编码问题 → 来回 6 轮。建议：第 3 次就该停下来要求 AI 整体分析根因，而不是继续逐个修。另一个经验：**修不动的时候先搜社区**，看是不是已知的框架 bug——死磕代码不如先确认"这条路到底通不通"。

> **换了方案但 AI 还在走老路**：讨论了很久方案 A，发现不通切到方案 B。AI 嘴上说"按 B 来"，代码里到处是方案 A 的影子。这个问题用纠正解决不了——唯一的办法是**换方案 = 换对话**，在新对话里从零描述方案 B，不要提方案 A。

> **多副本漂移**：一个配置文件在 3 个目录有副本，大小时间内容全不一致。AI 同步时把一个写成空文件，另一个乱码。建议在 KNOWLEDGE 里写明"权威版本在哪个路径"。

> **引入重复依赖**：处理日期这一件事，AI 在不同对话里引入了 3 种写法。建议 KNOWLEDGE 里列出已有依赖。

---

**正确做法③：每个修改点单独验证——没有正向效果就立刻回滚**

AI 经常一次改好几个地方，声称"整体优化"。但实际上有些改动有效、有些无效、有些甚至是负面的——混在一起你分不清哪个起了作用。

```
这次修改涉及多个改动点，逐个验证：
1. 先列出你这次一共改了哪些地方，每个改动的目的是什么
2. 每改一个点就跑一次测试，告诉我这个点改完后：
   - 目标指标有没有变好？变好了多少？
   - 其他已有功能有没有受影响？
3. 如果某个改动没有正向效果，立刻回滚这个改动，不要留着
4. 最终只保留有正向效果的改动
```

**为什么要这么做**：

- AI 很喜欢"顺便"改几个地方，声称"一起优化了"。不逐个验证的话，你无法知道哪些改动真正有用
- 更常见的是：AI 改了 5 个点，其中 1 个修好了 bug，另外 4 个是无效甚至有害的"顺便优化"。混在一起你会误以为 5 个改动都是必要的
- 回滚时如果是混合提交，只能整体回滚，有用的改动也会丢掉

> 实际遇到的：让 AI 优化页面渲染性能，它一次性改了数据加载方式、组件结构、CSS 动画和缓存策略四个地方。测试后发现速度确实快了——但逐个排查发现只有缓存策略是有效的，组件结构的改动反而引入了一个新 bug，另外两个完全没有效果。

---

**正确做法④：管好上下文**

- **切话题 = 开新对话**——不要在同一个对话里做两件不相关的事
- **感觉 AI 变蠢了**——用 `/compact` [CC] 或开新 Composer [Cursor]
- **出错想回退**——用 `/rewind` [CC] 或 Checkpoint [Cursor]；也可以让 AI 用 git 回退到上一个 commit
- **纠正过的错 AI 不记住**——别再说第二次，直接往 rules 里加一条
- **@folder 引用时 AI 只拿到摘要**——精确指向用 `@具体文件名`

**跨对话交接**——每次结束对话前让 AI 写交接摘要，下次开新对话直接粘贴为第一条消息：

```
这次对话要结束了。帮我写一段交接摘要，下次新对话直接粘贴就能接上：
1. 当前做到了哪一步（已完成 / 进行中 / 未开始）
2. 还剩什么没做
3. 遇到了什么问题、卡在哪
4. 下次开工的建议起点
格式简洁，不超过 20 行。
```

> 实际遇到的：不写交接摘要的话，新对话 AI 完全不知道"上次做到哪了"，要花 10-15 分钟重新解释。有交接摘要的话，30 秒就能接上。

---

**正确做法⑤：每步同步——确认 AI 真的存档了**

sync.mdc 规则会让 AI 每次改完自动更新 KNOWLEDGE 日志 + 提交 git。但 AI 有时候会"忘了"或"跳过"。你的确认方法：

```
刚才的改动：
1. git commit 了吗？备注写的什么？
2. KNOWLEDGE.md 的修改日志更新了吗？
3. 有没有产生临时文件需要清理？
```

**git 日常操作**——你不需要记命令，让 AI 帮你做，但要知道什么时候该做：

| 你想做什么 | 让 AI 做什么 |
| --- | --- |
| 看 AI 到底改了哪些文件 | "列出最近一次 commit 改了哪些文件，每个文件改了什么" |
| 回到上一个能跑的版本 | "帮我回退到上一个 commit" |
| 对比现在和之前有什么不同 | "对比当前代码和上一个 commit 的差异" |
| 做一个重要节点的存档 | "把当前状态提交 git，备注写'___ 功能完成'" |
| 想试一个不确定的方案 | "帮我建一个新分支叫 test-xxx，在新分支上试" |
| 试完不行想放弃 | "切回主分支，删掉 test-xxx 分支" |

> 实际遇到的：每个对话产生的调试脚本全丢在根目录，从不清理。后期根目录 400+ 个文件。跨对话后 AI 也找不到"上次做到哪了"。

---

**翻车急救**


| 情况            | 你说什么                    |
| ------------- | ----------------------- |
| AI 改了一堆不知道对不对 | "列出改了哪些文件、每个改了什么、为什么"   |
| 想回到之前能跑的状态    | "帮我回到上一个能跑的版本"          |
| 回退了不确定丢了什么    | "对比当前版本和回退前的差异"         |
| 完全搞不定了        | 关对话，开新的，带上报错和 KNOWLEDGE |


#### ✅ 段 3 完成

功能跑通 + KNOWLEDGE 已同步 + 代码已提交

---

### 段 4 · 验证

目标：确认 AI 说的"做完了"是不是真的做完了。

---

**正确做法①：要真实输出，不信口头承诺**

AI 说"做完了"的时候，用这个流程验证：

```
功能做完了，走验证流程：
1. 把你跑的命令和完整输出贴给我看（不要总结，贴原始输出）
2. 你自己判断这个功能应该测哪些场景（正常的、异常的、边界的），逐个跑并贴结果
3. 对照 KNOWLEDGE 里的验收标准，逐条打勾：达标/未达标
4. 红线功能我自己试，你等我确认
```

**关键区分：哪些你自己试，哪些让 AI 试**

| AI 测就行 | 你必须自己试 |
| --- | --- |
| 代码能不能跑起来 | 涉及真实用户数据的功能 |
| 基本功能是否正常 | 涉及付费/扣费/发通知的操作 |
| 错误处理是否生效 | 涉及权限/登录/安全的功能 |
| 格式和显示是否正确 | 最终用户体验（自己当一次用户走一遍） |

> 实际遇到的：AI 直接调 proxy API 说"通过了"，但实际用户走的是完整链路（飞书→Gateway→插件→proxy）。中间的超时、重试、去重全没测。AI 的测试报告全是"通过"，人一跑全是 bug。

---

**正确做法②：复杂项目让 AI 搭建测试体系**

手动验证每次改动太累，也容易漏。中大项目应该让 AI 帮你搭一套自动化测试，以后每次改完代码跑一遍就知道有没有搞坏别的东西。

**单元测试**——测单个模块是否正常工作（改了 A 模块，A 模块的功能还对不对）：

```
帮我给 ___ 模块写单元测试：
1. 你自己分析这个模块的核心功能点，判断每个点需要测哪些场景
2. 测试必须能实际跑通，跑完贴出完整输出
3. 把测试命令写进 KNOWLEDGE.md 的"常用命令"
以后每次改了这个模块，都要重跑这些测试。
```

**端到端测试（E2E）**——测整个流程从头到尾是否走通（用户视角：点击→处理→看到结果）：

```
帮我写一个端到端测试，模拟真实用户的完整操作流程：
1. 你自己判断用户最关键的使用路径是什么，从入口走到结果
2. 不只看最终结果，关键中间步骤也要验证
3. 跑一次给我看完整过程和结果
```

**什么时候该跑测试**：

| 场景 | 跑什么 |
| --- | --- |
| 改了某个模块的代码 | 该模块的单元测试 + 调用该模块的模块的测试 |
| 修了一个 bug | 先写一个能复现 bug 的测试 → 修 → 跑测试确认修好了 → 跑全部测试确认没搞坏别的 |
| 做了一次"优化" | 跑全部测试对比优化前后，没有任何指标变差才算有效优化 |
| 准备提交或上线 | 跑完整 E2E 测试走一遍全流程 |

> **关键纪律**：AI 不许修改已有测试来让测试通过（testing.mdc 已约束）。如果测试挂了，要么是代码有 bug 该修代码，要么是需求变了要先跟你确认再改测试。

> 实际遇到的：Figma 项目中 AI 偷偷调低了"像素级对比"的阈值来让更多测试"通过"，实际质量根本没变。如果测试标准写死在测试代码里且有 testing.mdc 约束，这种事就做不了。

---

**正确做法③：识别 AI 在糊弄你**

#### AI 的 18 种糊弄方式（速查表）


| #   | 模式          | AI 怎么表现                          | 你怎么识别                      |
| --- | ----------- | -------------------------------- | -------------------------- |
| 1   | **嘴上通过**    | "should pass"                    | "跑了什么命令？完整输出贴给我"           |
| 2   | **降低标准**    | "simplified for robustness"      | "改动的前后对比给我看"               |
| 3   | **改测试迁就代码** | "updated test to match behavior" | "是代码对了还是你把标准降了？"           |
| 4   | **只测正常情况**  | "looks good"                     | "异常场景的测试在哪？举 3 个"          |
| 5   | **偷偷跳过失败**  | 加了 skip 标记                       | "有被跳过的测试吗？全列出来"            |
| 6   | **表面修复**    | "已修复""不报错了"                      | "修前什么行为？修后什么行为？"           |
| 7   | **虚假优化**    | "优化了结构"                          | "去掉这次改动功能会出什么问题？"          |
| 8   | **编造依赖**    | 引用不存在的库                          | "给我官方文档链接"                 |
| 9   | **散弹枪修改**   | 5+ 文件每个改 1-2 行                   | "这是一件事还是好几件？"              |
| 10  | **校验代替产出**  | 你要优化效果，它加校验                      | "用户体验有什么变化？"               |
| 11  | **上下文走老路**  | 换了方案代码还用旧的                       | 停对话 → 开新的从零说               |
| 12  | **目标漂移**    | 你要优化方法，它修产物                      | "对下一个项目也有效吗？"              |
| 13  | **流程跳步**    | 偷偷跳过某步                           | "第 X 步完整产物在哪？"             |
| 14  | **浅层执行**    | 只看顶层就写报告                         | "所有子项都查了吗？列出来"             |
| 15  | **规则毒化**    | 错误规则导致后续全失败                      | 定期让 AI 审计规则文件              |
| 16  | **多副本漂移**   | 同一文件多处内容矛盾                       | "有其他副本吗？一起改"               |
| 17  | **重复依赖**    | 又装了一个做同样事的库                      | "有已有依赖能做吗？"                |
| 18  | **密钥硬编码**   | 密码写进代码                           | 搜 `sk-` `password` `token` |


几个实际遇到的验证阶段典型问题：

> **偷偷降标准**：要求"30 轮、每组 10 轮、20 个 case"的测试，AI 理解成了完全不同的结构，覆盖大幅缩水。
>
> **浅层执行**：让 AI"全面 review 服务器文件"，它只看了 3 个主文件就写了报告。深入之后才发现 3 个实质矛盾。
>
> **目标漂移**（Figma 项目）：要求优化生成方法，AI 反复去调某个文件里按钮的坐标——改了 10 轮，方法没变。
>
> **改测试迁就代码**（Figma 项目）：AI 调低了"像素级对比"的标准让更多测试"通过"，某些结果被标记为"暂不对比"直接跳过。
>
> **规则毒化**（Figma 项目）：规则文件写了"优先用某字体"，但工具不支持该字体，每次执行都必然失败。这条规则藏了好几轮才被发现。

---

**正确做法④：关键功能让 AI 扮演"找茬者"审一遍**

```
换个角色：你是专门找茬的审查员。审查刚才做的 ___ 功能：
1. 根据这个功能的特点，你自己判断应该从哪些角度审查（安全、性能、边界情况、数据一致性等）
2. 搜一下代码里有没有 sk- / password / token / secret 硬编码
3. 有没有我让做但你跳过了的步骤？
4. 你"顺便"改了什么我没要求改的？
5. 测试标准有没有被降低过？
逐条回答，有问题的列出具体文件和行号。
```

> 同样的原则：不要替 AI 限定审查角度，除非你确实知道重点在哪。AI 对代码的理解比你深，让它自己判断该查什么，反而能找到你想不到的问题。

#### ✅ 段 4 完成

每条"完成"有可追溯的真实输出 + 红线功能你自己试过

---

### 段 4.5 · 搭建自测自优化能力（进阶）

目标：搭一套"跑 N 轮 → 评分 → 优化 → 回归"的闭环，适用于需要反复迭代质量的场景。

**六步流程**：

**1. 设计回测轮次**——每轮从零独立执行，不许在上一轮基础上改，保存完整产出物。

> 实际遇到的：10 轮回测，AI 从第 3 轮开始偷偷在上一轮基础上改，到第 7 轮才发现。"每轮从零"和"出示完整产物"必须写死在规则里。

**2. 建立评分标准**——必须有量化对比基准，不许 AI 自评（"看起来不错"不算数）。

> 实际遇到的：AI 做了"自评"而不是严格对比参考标准，还调低了对比阈值让更多测试"通过"。

**3. 优化改方法不改产物**——优化目标是通用方法（下次也能用），不是修当前产物的具体数值。

> 实际遇到的：AI 反复调某个按钮的坐标——改了 10 轮，通用方法一点没变。

**4. 回归验证**——优化后先重跑一遍，确认之前通过的指标没有变差。

> 实际遇到的：一次优化导致 8 个状态全部退步。没有回归验证的话退步会越滚越大。

**5. 限制自动轮次**——每 3 轮停下来人工审查方向，连续 2 轮没提升就停下讨论。

> 实际遇到的：跑了 13 轮后发现方法论本身有结构性缺陷。第 3 轮就该停下来审查。

**6. 定期做架构讨论**——不只是调参数，要审视方法论本身有没有问题。

> 实际遇到的：瓶颈不在生成结果，而在中间产物（PRD 理解）的深度不够——之前 13 轮一直在优化最后一步，瓶颈其实在第二步。

**启动回测的开口方式**：

```
我要对 ___ 做迭代优化。规则：
1. 每轮从零独立执行，不许在上一轮产出基础上改
2. 每轮必须保存完整产出物，命名带轮次号（如 v1/ v2/ v3/）
3. 用 ___ 做量化评分（不许自评"还不错"，必须有数字对比）
4. 每 3 轮停下来给我看趋势，我决定要不要继续
5. 连续 2 轮分数没提升就停，一起分析是不是方向有问题
6. 优化的是方法（下次也能用），不是调这次的具体数值
先跑轮 1 和轮 2 作为基线。
```

**整体节奏**：

```
轮 1-2：基线执行 → 轮 3：停，审查方向 → 轮 4-5：优化后执行
→ 回归验证 → 轮 6：评估继续或收工 → 重复 → 到上限收工，固化方法论
```

#### ✅ 段 4.5 完成

有量化评分标准 + 至少跑过 1 轮回归 + 优化的是方法不是产物

---

### 段 5 · 沉淀

目标：让今天踩的坑明天不再踩。短期项目把教训写进 KNOWLEDGE 就行。

**结项体检**——先让 AI 扫一遍项目状态：

```
帮我做项目体检：
全面扫描项目现状，你自己判断该检查哪些方面（文件清理、文档一致性、规则冲突、遗留问题等）。
给一个整体健康度评估：什么状态好、什么有隐患、建议处理的优先级。
```

**收尾整理**——把经验固化下来：

```
帮我做收尾：
1. 把这次踩过的坑、做过的关键决策、总结的教训追加到 KNOWLEDGE.md
2. 如果有反复纠正 AI 的问题，检查是否已经写进 rules（没有就补上）
3. 更新 KNOWLEDGE.md 的"常用命令"部分（启动、测试、部署的完整命令）
4.（如需复用）抽出可复用的配置做模板
5.（如需交接）写一份上手指南：项目是什么、怎么跑起来、改东西从哪里入手
```

**判断沉淀是否到位的标准**：假设明天换一个人（或者你自己三个月后回来），开一个新对话，给 AI 读 KNOWLEDGE + rules，它能不能在 30 分钟内接上手？如果不能，缺什么就补什么。

> 实际遇到的：跨对话的经验断裂严重——做到一半的工作散落各处，换对话后 AI 找不到"上次做到哪了"。KNOWLEDGE 一开始写在临时文件里，每次新对话还是忘。后来才意识到应该写进永久规则。

#### ✅ 段 5 完成

换台电脑或换个人，能在半小时内接着干

---

## 拓展 · 后续自学方向

### 黑盒用 AI 还是白盒用 AI？

大多数人用 AI 是**黑盒方式**——把需求丢进去、拿结果出来，不关心中间发生了什么。

这份手册教的是**白盒方式**——你不需要会写代码，但你知道 AI 在做什么、为什么这么做、在哪里可能出错。


|      | 黑盒             | 白盒                       |
| ---- | -------------- | ------------------------ |
| 心态   | "AI 帮我搞定就行"    | "AI 做，我判断"               |
| 出问题时 | 不知道哪里错了，重试或放弃  | 知道可能是哪类问题，能给 AI 指方向      |
| 天花板  | 简单任务能完成，复杂任务卡住 | 能突破复杂任务，因为你能介入 AI 做不好的部分 |


不需要变成专家——从纯黑盒往白盒方向走一步，效果就会不同。下面这些方向，每个花 10 分钟了解就够：


| 方向                 | 了解后能解决什么                  | 怎么学                                           |
| ------------------ | ------------------------- | --------------------------------------------- |
| LLM 的 attention 机制 | 理解为什么换方案要换对话、为什么聊久了 AI 变蠢 | 搜"context engineering"；Anthropic 官方 prompt 指南 |
| 上下文工程              | 理解 KNOWLEDGE/rules 的设计本质  | Cursor 官方 blog；搜"LLM context window"          |
| 软件工程基础             | 理解为什么要拆模块、为什么改一处崩三处       | B 站搜"软件工程 入门"；或直接让 AI 教你                      |
| 代码化 vs LLM 化       | 知道哪些逻辑该用代码、哪些该让 LLM 灵活处理  | 实践中积累；搜"when to use LLM vs code"              |
| 数据库基础              | 理解为什么数据库操作是高风险红线          | B 站搜"数据库 入门"                                  |
| 版本管理 git           | 会用分支、diff、revert          | B 站搜"git 入门"；"Learn Git Branching" 互动教程       |


**怎么学最高效**：

1. 让 AI 当老师——直接问"用我能理解的方式解释 X"
2. B 站/YouTube 找 10 分钟入门视频——不要找 3 小时课程
3. 边做边学——遇到不懂的概念时再查，记忆最深
4. 把学到的写进 KNOWLEDGE——下次 AI 也能看到

---

## 第三部分 · 速查附录

### 附录 A · 万能开口模板

```
1. 我是谁、要解决什么
2. 先了解项目现状，别急着动手
3. 给方案让我看，讨论完再动手
4. 一步步做，每步等确认
5. 做完验证，把经验写进 KNOWLEDGE
```

### 附录 B · 调研清单（设计阶段让 AI 做功课）


| 调研项  | 让 AI 做什么                          | 为什么           |
| ---- | --------------------------------- | ------------- |
| 官方文档 | 查框架/库最新版 API 和限制                  | AI 记忆中的用法可能过时 |
| 工具指南 | 查 Cursor/Claude 官方 best practices | 工具最佳实践在更新     |
| 社区项目 | 搜 GitHub/论坛找类似项目                  | 别人踩过的坑不用再踩    |
| 论文   | 如涉及 AI/LLM，查相关研究                  | 了解能力边界        |
| 已知坑  | 综合以上列出最可能的坑                       | 提前避坑          |


### 附录 C · 按项目规模选配


| 产物              | 小项目 | 中项目 | 大项目 |
| --------------- | --- | --- | --- |
| KNOWLEDGE.md    | 简版  | 标准版 | 完整版 |
| 安全规则            | ✅   | ✅   | ✅   |
| ARCHITECTURE.md | —   | 简版  | 完整版 |
| 红线文档            | —   | ✅   | ✅   |
| 验收标准            | —   | ✅   | ✅   |
| 回归测试集           | —   | —   | ✅   |
| 团队模板            | —   | —   | ✅   |


> 小=课程作业/小工具　中=内部系统/MVP　大=长期上线产品

### 附录 D · AI 谎言速查词典


| AI 说              | 大概率   | 你问              |
| ----------------- | ----- | --------------- |
| "should pass"     | 没跑    | "跑了什么？结果？"      |
| "looks good"      | 没细看   | "贴异常场景测试"       |
| "理论上/应该"          | 在猜    | "实际跑了吗？"        |
| "我以为"             | 在猜    | "依据？"           |
| "已修复"             | 没复测   | "重跑一次给我看"       |
| "顺便优化了"           | 跑偏    | "回滚"            |
| "优化了结构"           | 无效改动  | "去掉会出什么问题？"     |
| "升级到更好方案"         | 没有对比  | "什么指标证明更好？"     |
| 你没见过的库名           | 可能编的  | "给我官方链接"        |
| "清理了冗余代码"         | 删了安全网 | "删了什么？逐条说"      |
| 改了 5+ 文件每个 1-2 行  | 散弹枪   | "这是一件事吗？拆开做"    |
| "增加了校验"（你要的是优化）   | 偷换需求  | "用户体验变了吗？"      |
| 代码出现旧方案元素         | 上下文污染 | 停对话 → 开新的       |
| "基于上一轮微调"         | 偷跳步   | "是独立做的还是在上轮改的？" |
| 报告只有顶层结论          | 浅层执行  | "所有子项都查了吗？"     |
| 做了 10 轮只对当前有效     | 目标漂移  | "对下个项目也有效吗？"    |
| 同一文件多目录不一致        | 多副本   | "权威版本在哪？"       |
| 又装了一个类似的库         | 重复依赖  | "已有依赖能做吗？"      |
| 代码出现 sk-/密码/token | 密钥硬编码 | 立刻删 → 用环境变量     |


### 附录 E · 全周期检查清单


| 段    | 完成标志                       | 没做的风险             |
| ---- | -------------------------- | ----------------- |
| 1 启动 | KNOWLEDGE + git + rules 已建 | AI 反复失忆、密钥泄露、无法回退 |
| 2 设计 | 模块拆分对齐 + 红线已列              | 改一处崩三处            |
| 3 搭建 | 功能跑通 + 已同步 + 已提交           | 进度丢失              |
| 4 验证 | 每条有真实输出 + 红线人工试过           | 上线后发现问题           |
| 5 沉淀 | 换人能半小时接着干                  | 经验留不下来            |


### 附录 F · 术语速查


| 术语                  | 大白话                 |
| ------------------- | ------------------- |
| KNOWLEDGE.md        | 项目备忘录，AI 每次开工先读     |
| .cursor/rules/      | 给 AI 立的规矩，自动遵守      |
| 接口约定                | 模块的"门"——接受什么、输出什么   |
| 模块                  | 项目拆成的一块块独立功能        |
| 红线                  | 绝对不能出错的操作或数据        |
| git / commit        | 存档，可回退到任意存档点        |
| Plan Mode           | AI 只规划不写代码的模式       |
| `/compact` [CC]     | 压缩对话历史恢复注意力         |
| Checkpoint [Cursor] | 自动存档点，可回退           |
| 上下文污染               | 旧方案讨论影响了 AI 对新方案的执行 |


---

## 今天回去可以试的 3 件事

1. 打开你正在做的项目，让 AI 帮你建一份 KNOWLEDGE.md（prompt 在段 1，直接复制）
2. 下次 AI 说"已完成"时，问它一句："你跑了什么命令？把完整输出贴给我看"
3. 翻一遍附录 D 的谎言速查词典，看看有没有你遇到过的

有问题随时交流。手册文档会发出来，大家按需参考。