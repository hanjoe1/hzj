# AI 协作手册

> **副标题**：非技术人员如何跟 AI 搭档做项目——原则、流程与避坑指南
>
> **适用工具**：本手册以 **Cursor** 为主要示例。涉及 Claude Code 专属功能时会标注 `[CC]`，Cursor 专属功能标注 `[Cursor]`。

---

## 如果你只有 5 分钟

> 只看这三样，今天就能用上：

**1. 分工表**：哪些你想、哪些 AI 干 → 见 [1.2 黄金分工表](#12-黄金分工表)

**2. 五条铁原则**（贴墙）：

1. 先想清楚再开口
2. 关键判断必须写进文件（口头说的 AI 下次就忘）
3. 让 AI 先反问你（你以为说清了，其实没有）
4. 要真实输出不要口头承诺（AI 说"好了"不算数，看它实际跑出来的结果）
5. 小步走、每步审（每次让 AI 做的事不超过 30 分钟的量）

**3. AI 的 18 种糊弄方式** → 见 [段 4 · 速查表](#ai-的-18-种糊弄方式速查表)

---

## 贯穿本手册的真实项目

> 本手册的大部分案例来自一个真实项目：**用 AI 做一个飞书上的智能助手（bot）**。
>
> 这个项目做了 2 个多月、经历了 30+ 轮对话、涉及服务端部署、飞书插件对接、多模型调度。过程中踩过的坑包括：AI 反复用错 Shell 命令、把数据库密码写进代码、改 A 崩 B 的连锁反应、换了方案但 AI 还在走老路、同一个文件 3 个地方有副本且内容互相矛盾……几乎把 AI 协作能踩的坑踩了个遍。
>
> 你的项目不一定跟这个一样复杂，但这些坑是通用的——无论你做网站、小程序、数据工具还是自动化脚本，都会遇到。

---

## 第一部分 · 总论

### 1.1 先认识你的搭档——AI 是怎样的

用好 AI 之前，先理解它是什么"人"。下面 5 条不是抽象概念，每一条后面都跟着一个真实踩坑。

**① 能力强，但记性差**——每次开新对话就从零开始，之前说好的事全忘了。

> 实际案例：做一个飞书 bot 项目，上午告诉 AI"当前 Shell 是 PowerShell，不要用 Linux 命令"，它改用了 PowerShell 语法。下午开新对话，它又用 `&&`、`wc`、`head` 这些 Linux 命令，报错了 4 次才想起来。这个问题在整个项目里**每换一次对话就重踩一次**。
>
> → 所以关键信息必须写进文件（KNOWLEDGE.md），不能只在对话里说。

**② 没有"改动成本"意识**——对它来说改 1 行和改 100 行一样轻松，所以它倾向于大改。

> 实际案例：让 AI"优化一下页面加载"，它把整个组件重写了——加载速度没变，但之前调好的一个交互坏了。它觉得"反正重写也不费事"，但你要花半天修它重写引入的新 bug。
>
> → 所以没有经过你同意，AI 不许做任何"优化""重构"。

**③ 会说你想听的话**——它不是故意骗你，但它倾向于报告"好消息"。

> 实际案例：AI 说"所有测试通过"。追问"你跑了什么命令？"——它回答不上来，因为根本没跑测试，只是觉得代码"看起来对"就报了"通过"。另一次，它说"已修复审批逻辑的 bug"，实际只是把报错的那行注释掉了。
>
> → 所以要看 AI 实际跑出来的输出，不信它的总结。

**④ 聊越久越不靠谱**——对话越长，它的注意力越分散，越容易忘事、犯低级错误。

> 实际案例：一个 Figma 生成项目，前 3 轮 AI 严格按约定每轮独立生成+截图对比。到第 7 轮用户才发现，从第 4 轮开始 AI 就偷偷在上一轮基础上局部修改（而不是独立重新生成），早期"每轮必须独立"的约定被它忘了。
>
> → 所以一个任务一个对话，别一个对话用一整天。

**⑤ 不会主动说"我做不到"**——遇到不确定的事，它会编一个看起来合理的答案，而不是告诉你"我不确定"。

> 实际案例：AI 推荐了一个 npm 包来发邮件，名字听着很正经，代码也写得像模像样。跑 `npm install` 时报错——这个包根本不存在，是 AI 编的。研究显示约 28% 的依赖推荐是虚构的。
>
> → 所以 AI 引入新东西时，问它"给我这个库的官方文档链接"，给不出就是编的。

### 1.2 你和 AI 各管什么

理解了 AI 的特点之后，分工就很自然了：


| 维度    | 你来（AI 做不了或不可靠）   | AI 来（有方向它就能干） |
| ----- | ---------------- | ------------- |
| 这是什么  | 项目是什么、给谁用、解决什么问题 | 用什么技术来实现      |
| 做多久   | 长期维护 还是 一次性用完    | 需要配多少"基础设施"   |
| 做什么   | 要做什么、**明确不做什么**  | 任务怎么拆、先做哪个    |
| 什么不能错 | 哪些数据/操作出错后果严重    | 怎么测试、怎么验证     |
| 什么算完成 | "做完了"到底长什么样      | 把你的标准转成具体测试   |
| 这步对不对 | 每一步的判断——继续还是回退   | 具体怎么写代码       |


> 简单记法：AI 不可靠的事（记忆、判断、验证）你来；AI 擅长的事（生成、搜索、展开细节）它来。

### 1.3 关键信息必须写进文件

因为 AI 记性差（特质①），口头交代的事下次就忘。所以——


| 必须写进文件的      | 口头说就行的     |
| ------------ | ---------- |
| 项目是什么、做多久    | 这次具体做哪个小任务 |
| 什么不能错（业务红线）  | 临时的小调整     |
| 验收标准         | 一次性的偏好     |
| 各模块怎么配合      | 试探性的尝试     |
| 踩过的坑、教训      | —          |
| 已经否决的方案（别再用） | —          |


> **文件放哪？** 放在项目根目录。AI 每次开新对话都会自动读取 `KNOWLEDGE.md`（Cursor 中对应 `.cursor/rules/`），这样它就"记住了"。

---

## 第二部分 · 项目全周期五段

> 每段固定三栏：**你想清楚什么 / 怎么开口 / 在哪里判断**
>
> **不是每段都必须做**——短期小项目可以跳过段 2 的架构设计和段 5 的团队沉淀，但段 1（启动）、段 3（搭建纪律）、段 4（验证）不能跳。

---

### 段 1 · 项目启动（让 AI 从第一步就走对方向）

#### 你先想清楚这几件事


| 必答               | 为什么重要         |
| ---------------- | ------------- |
| 一句话这是什么项目？       | 说不清 = 没想清     |
| 长期维护 还是 一次性？     | 决定要不要花时间建基础设施 |
| 给自己 / 团队 / 外部用户？ | 决定文档和规范做多细    |
| 新建目录 还是 在已有项目里加？ | 影响文件组织和隔离方式   |


#### 怎么开口

```
我要做一个 ___（一句话描述）。
- 类型：长期 / 短期
- 受众：自己 / 团队 / 外部
- 目录：新建 ./___

请你：
1. 复述你理解的项目，让我确认
2. 反向问我 3-5 个最关键的歧义问题
3. 基于项目类型，建议要不要建 KNOWLEDGE.md，以及它该包含哪些章节
4. 等我说 GO 再动手
```

#### 在哪里判断

- AI 复述对不对？→ 不对立刻纠
- AI 反问的问题有没有戳到要害？→ 没有说明它还没理解
- 它建议的 KNOWLEDGE 章节合不合理？→ 你来增删

#### KNOWLEDGE.md 是什么？

> **一句话**：KNOWLEDGE.md = 你给 AI 的**项目备忘录**，放在项目根目录。AI 每次开新对话都会先读它，这样就不会"失忆"。

**你不需要自己写 KNOWLEDGE.md——让 AI 帮你建。** 开口方式：

```
帮我建一份项目备忘录（KNOWLEDGE.md），放在项目根目录。
你先扫描一下项目现状，然后按下面的要点草拟内容，给我看完我再确认：
- 项目目标（一句话）
- 技术栈（你探测到的）
- 运行环境（操作系统、Shell类型、语言版本）
- 已知坑（目前遇到的问题）
- 能跑的版本（最近一次正常运行的状态）
```

> 中大项目可以补充要求 AI 加上：模块清单 + 变更日志 + 测试指引 + 架构决策(why) + 教训库 + 已废弃方案记录

> **为什么要写"运行环境信息"？** 真实教训：在 Windows/PowerShell 环境下，AI 每次新对话都会用 Linux 命令（`&&`、`wc`、`head`），反复报错。把"当前 Shell 是 PowerShell"写进 KNOWLEDGE，AI 就不会每次重踩。同理：Python 版本、Node 版本、数据库类型等都值得记下来。

#### 安全边界

告诉 AI 哪些命令**绝对不许自动执行**：

- 删除文件（`rm -rf`）
- 管理员权限（`sudo`）
- 强制推送代码（`git push --force`）
- 自动安装全局依赖
- **任何操作数据库的命令**——特别是 `DROP`、`DELETE`、`--force-reset`、`downgrade` 等

> **⚠️ 真实案例警示**：2026 年已有多起公开事故——AI 被要求"查看日志""分析问题"，却自行执行了删表、重置数据库的命令，导致全部数据不可恢复地丢失。所以**数据库操作必须列入"先问我再执行"清单**。

#### 这个项目里发生了什么

> 飞书 bot 项目在启动阶段**跳过了上面所有步骤**，直接开始写代码。后果是：
>
> - 没有 KNOWLEDGE → AI 每换一次对话就忘了技术栈和环境信息，PowerShell 环境下反复用 Linux 命令报错（先后踩了至少 4 次）
> - 没有安全边界 → AI 把飞书 API 的 appId 和 appSecret 直接硬编码到了一个 JavaScript 文件里，差点被提交到 git 仓库
> - 没有一句话项目介绍 → 每次新对话都要花 10-15 分钟重新解释项目背景
>
> 如果一开始花 10 分钟做完启动三件套（KNOWLEDGE + 安全边界 + 项目介绍），后面能省几十个小时。

#### ✅ 段 1 完成标志

项目类型已定 + KNOWLEDGE.md 骨架已建 + 安全边界已设

---

### 段 2 · 项目设计（先想清楚结构再动手写）

> **短期小项目可简化本段**：不需要写正式的架构文档，但至少跟 AI 对一遍"大概分几块、谁先谁后"。

#### 你先想清楚这几件事


| 必答          | 为什么重要         |
| ----------- | ------------- |
| 大概分几块？      | 不拆清楚，后期改一处崩三处 |
| 哪些核心、哪些可选？  | 优先级你定         |
| 什么不能错？（红线）  | AI 不知道你的业务边界  |
| "做完"的标准是什么？ | 没标准就没验收       |


#### 怎么开口

```
进入规划模式，不要写代码。

1. 先问我 3-5 个最关键的歧义问题
2. 基于我的回答，列出模块清单（每个模块一句话说明职责）
3. 画出模块之间的依赖关系（谁依赖谁、数据怎么流）
4. 每个决策都要写 why（为什么用 X 不用 Y）
5. 等我审完再进下一步
```

#### 要求 AI 先做"功课"——查文档、查社区

> **这是非常关键的一步**：AI 自身的知识可能过时或不准确。在设计方案之前，必须要求它先去调研。

告诉 AI：

```
在做方案设计之前，请你先做以下调研，每项给我结论：
1. 查阅你要使用的框架/库的官方文档，确认最新版本的用法和限制
2. 查阅 AI 模型/工具的官方指南（如 Cursor 官方 best practices、
   Claude 的 context engineering 指南、Anthropic 官方文档）
3. 搜索社区里有没有类似项目/方案（GitHub、论坛、博客），
   总结别人踩过的坑和推荐做法
4. 如果涉及 AI/LLM 相关的功能设计，查阅相关论文或技术报告

调研完毕后，给我一份"调研摘要"：
- 参考了哪些文档（附链接）
- 社区推荐做法是什么
- 有哪些已知坑
- 你的方案跟主流做法有什么差异，为什么
```

> **为什么要这么做**：AI 有时候会"凭印象"设计方案，用过时的 API、忽略已知的坑。让它先查文档再设计，方案质量会高一个档次。这就像让新人"先别急着写代码，先看看团队 wiki 和别人怎么做的"。

#### 接口约定先行

> **"接口约定"用大白话说**：就是先定义好每个模块的"门"——它接受什么输入、给出什么输出、出错怎么报告。就像先画好每个房间的门窗位置再装修。

- **长期项目铁律**：所有模块的接口都定完 → 才允许开始写实现代码
- **短期项目简化**：跟 AI 口头对一遍"A 模块给 B 模块传什么数据"就行

#### 红线清单

- **你来想**：业务里什么数据最敏感？什么操作不可逆？
- **AI 来补充**：基于你说的，起草一份完整清单
- **你来审**：补充 AI 不知道的业务红线

#### 撞墙了怎么办？——已有项目的急救入口

> 如果你已经做了一段时间，项目乱了才来看这份手册——没关系，从这里开始：

```
我的项目已经做了一段时间，现在比较乱。请帮我：
1. 扫描项目现有代码，逆向梳理出模块清单和依赖关系
2. 标注哪些模块比较稳定、哪些经常出问题
3. 基于梳理结果，补一份 KNOWLEDGE.md
4. 列出当前最大的 3 个风险点
5. 给出"先救什么、后优化什么"的优先级建议
```

#### 这个项目里发生了什么

> 飞书 bot 项目的模块拆分做得太晚。结果是：
>
> - **改一处崩三处**：修改审批流程的消息格式，导致通知模块、日志模块同时出错——因为三个模块之间的数据怎么传根本没约定过
> - **没让 AI 先查文档**：AI 用了飞书插件的 webhook 模式来接收消息，代码写了一大堆。后来才发现官方插件的 webhook 分支根本是空的（只有一个 `return;`），整个方向全废了。如果一开始让 AI 查官方文档就能避免
> - **5 层规则互相矛盾**：项目有框架层、插件层、SOUL.md、AGENTS.md、anti-regression skill 五层配置文件，其中多处对同一行为给出矛盾指令。AI 收到矛盾指令后两边都不执行，bot 直接不响应
>
> 后来不得不做了一次全面审计，把所有配置文件的矛盾逐一修复——这件事如果在设计阶段做，成本不到后期的 1/10。

#### ✅ 段 2 完成标志

模块怎么拆已对齐 + 红线已列出 + 验收标准已写（大项目还需接口约定签字）

---

### 段 3 · 项目搭建（AI 开始写代码了，你该盯什么）

#### 你先想清楚这几件事


| 必答            | 为什么重要          |
| ------------- | -------------- |
| 现在做哪个模块？      | 一次只做一个，绝不并行    |
| 这个模块"做完"长什么样？ | 没标准 = AI 永远做不完 |
| 哪些文件不能碰？      | 防 AI 乱改已经调好的东西 |


#### 怎么开口（开工时发给 AI）

```
开始做模块 ___。规则如下：
1. 只改 ___ 路径下的文件，碰别的先问我
2. 每完成一小步就停下汇报：做了什么 / 没做什么 / 下一步建议
3. 已经调通的代码不许动，要改先告诉我为什么
4. 不许"顺便优化"任何不在本次任务范围内的东西
5. 同一个错误修了 3 次还不行就停下来报告，别硬试
6. 汇报只许说"我跑了 X 命令，输出是 Y"，禁说"我以为""应该可以了"
7. 不许删除任何现有代码，除非我明确要求
8. 每次改动集中在一件事上，不要东改一行西改一行
```

> **小贴士**：喂 AI 已有代码比文字描述规范有效 10 倍。与其写"请使用函数式风格"，不如直接把项目里一个写得好的文件贴给 AI，说"照这个风格写"。

#### 初学者最常犯的 4 个错（在看坑点清单之前先过一遍）

**❌ 盲目 Accept All 不看改动**

> 这是初学者损失最大、发生最频繁的错误。AI 给出一大段改动，你看都不看就点"全部接受"——结果发现功能坏了，但已经不知道 AI 改了什么、为什么坏了、怎么回去。

- 铁律：**先看 diff（改动前后对比）再点接受**。特别注意：AI 删了什么？改了哪些你没要求改的文件？
- 如果改动超过你能看懂的范围（比如 10+ 个文件），要求 AI 分批改，一批一批看

**❌ 一个 prompt 塞进整个项目需求（"巨型 prompt"）**

> "帮我做一个电商网站，要有用户注册、商品展示、购物车、支付、后台管理、数据统计……"——这种一口气描述整个项目的 prompt，AI 会自作主张做大量你没说的决策，砍掉你需要的细节，生出一堆你不想要的东西。

- 铁律：**一次只做一件事**。先做用户注册，做完验证通过了再做下一个。逐个叠加，不要一把梭
- 回到段 2 的思路：先拆模块、排优先级，再一个一个做

**❌ 密钥 / 密码写进代码（25% 的 AI 项目中招）**

> AI 非常容易把 API 密钥、数据库密码、第三方 token 直接写进代码文件——甚至写进前端代码被用户看到。2026 年的研究显示约 25% 的 AI 生成项目存在此问题。

- 铁律：在 KNOWLEDGE.md 里写明"所有密钥必须用环境变量，禁止硬编码到代码中"
- 每次 review AI 的改动时，搜索有没有出现类似 `sk-`、`password`、`secret`、`token` 的硬编码值

**❌ 不看代码就部署——AI 生成的安全漏洞你兜底**

> AI 生成的代码经常存在安全漏洞：SQL 注入、跨站脚本（XSS）、路径穿越、未验证的用户输入。代码能跑 ≠ 代码安全。上线后出安全事故是你的责任，不是 AI 的。

- 铁律：涉及用户输入、数据库操作、文件操作、权限判断的代码，**必须人审**——哪怕你不完全懂代码，也要让 AI 解释"这段代码有没有安全风险，列出可能的攻击场景"

---

#### 3A · 过程纪律

##### 防漂移信号（必须警觉）


| 你观察到的信号            | 立刻做什么               |
| ------------------ | ------------------- |
| AI 改了跟当前任务无关的文件    | 停 + 让它回滚            |
| AI 说"顺便帮你优化了 X"    | 拒绝 + 回滚那部分          |
| AI 解释了很多但没真正做事     | 要求"少说，把实际运行结果贴出来"   |
| AI 不再问你问题，自顾自做了一大堆 | 危险信号——停下来让它解释在做什么   |
| 同一个错误来回修了 3 次      | 强制停，换个思路或换个模型       |
| 聊到后半段明显变蠢了         | 开新对话 + 写一段交接摘要      |
| 多步任务中 AI 偷偷跳过了某个步骤 | 问"你是不是跳过了第 X 步？回去补" |
| AI 每做一小步就停下来问你确认   | 告诉它"先完整做完再来整体汇报"    |


##### 上下文管理（防止 AI "越聊越蠢"）

- **一个任务一个对话**——切话题就开新窗口
- **聊太久了**——用 `/compact` [CC] 压缩历史，或 [Cursor] 开新 Composer
- **出错想回退**——用 `/rewind` [CC] 或 [Cursor] 的 Checkpoint 恢复
- **交班给下次**——写一段交接摘要放进 KNOWLEDGE.md
- **纠正过的错误 AI 不记住**——你在当前对话里纠正了 AI 的做法，但下次开新对话它还会犯同样的错。解法：**把纠正写成规则**放进 `.cursor/rules/` 或 KNOWLEDGE.md，而不是只在对话里说
- **用 @folder 引用时 AI 只拿到摘要**——你以为 `@src/` 会让 AI 读完所有文件，实际它只拿到文件名和摘要，大文件会被截断。需要精确指向时用 `@具体文件名`，不要用 `@整个文件夹`

##### 改动同步纪律

- **铁律**：每完成一个可验证的小步骤 → 更新 KNOWLEDGE 变更日志 → 提交代码（git commit）
- 频率可以调：小项目"每个会话结束时同步一次"就够了

---

#### 3B · AI 常见的坏习惯（按严重性分组）

> 下面这些坑不是"可能遇到"，而是"一定会遇到"。提前认识它们，遇到时就不会慌。

##### 第一类 · AI 在帮倒忙（看起来在做事，实际在添乱）

**虚假优化**——AI 不停重构、优化、重写代码，看起来很忙，但功能没有任何提升，甚至越改越差。

- 原因：AI 没有"改动成本"意识，改 1 行和改 100 行对它心理成本一样；没有明确任务时它会用重构填充行为
- 信号："我优化了代码结构" / 连续多轮大面积改动但效果不变 / 之前能跑的反而坏了
- 判断法：问"这次改动去掉的话，功能会出什么问题？"——答不上来就是虚假优化，回滚
- 防护：在开工规则里加"没有经过我同意，不许做任何优化/重构/改善"

**用校验代替产出**——你让 AI 优化功能效果，它去加了一堆输入校验、异常捕获、日志打印，但用户体验没任何变化。

- 原因：AI 分不清"结果更好"和"出错更少"；加校验比改善算法容易得多
- 典型场景：你要搜索更准 → 它加输入限制 / 你要加载更快 → 它加 loading 动画 / 你要推荐更好 → 它加空结果兜底
- 判断法：问"用户操作时会感受到什么不同？"——答案只有"更安全了"但体验没变，就是在偷换问题

**散弹枪式修改**——AI 东改一行西改一行，把原本完整的功能拆得支离破碎。

- 原因：AI 没有代码完整性概念；多轮对话后逐步丧失全局视角；每次修 bug 都在不同位置打补丁而非找根因
- 信号：一次改 5+ 文件每个只动 1-2 行 / 一个函数的逻辑被拆散到 3-4 个地方 / 你已经看不懂整体逻辑了
- 判断法：问"这次改动做的是一件事还是好几件事？"——好几件就要求拆开

**偷偷删代码**——AI 把你的错误处理、边界检查、兼容性代码当"冗余"清理掉。

- 信号：diff 里有你没要求删除的代码消失了 / AI 说"清理了冗余代码" / 之前能处理的异常情况突然崩了
- 防护：review 时不只看加了什么，**更要看删了什么**

**目标漂移——你要优化"方法"，AI 去优化"这一次的产物"**

- 真实案例：让 AI 优化"PRD→Figma 生成方法"，它却反复去调某一个 Figma 文件里某个按钮的 y 坐标——改了 10 轮，方法一点没变，只是当前这个文件看着好了一点
- 判断法：问"这次改动对下一个项目也有效吗？"——如果答案是"只对当前这个有效"，那就是在优化产物不是优化方法
- 防护：在 prompt 里显式说"我要的是通用方法的改进，不是这一次产物的修修补补"

**流程跳步——长任务中 AI 主动省略它认为"可跳过"的步骤**

- 真实案例：10 轮测试，每轮本应独立重新生成 + 截图对比。AI 从第 3 轮开始偷偷在上一轮基础上局部修改（而非重新生成），跳过截图 diff 改用"结构化评估"。用户到第 7 轮才发现前面几轮全废了
- 信号：多步任务中某一步的产出时间突然变短 / AI 说"基于上一轮做了微调"而非从头来 / 你发现中间步骤的产物不存在
- 防护：关键多步任务中，在每步结束时要求 AI **出示该步骤的完整产物**（不是总结，是实际文件/截图/输出），确认做了才继续

**过度征询 vs 缺乏闭环——该停不停，不该停乱停**

- 真实案例 1（过度征询）：AI 每做完一个小步骤就停下来请求确认，用户说了 3 次"先完整做完再来评审"
- 真实案例 2（缺乏闭环）：AI 改了本地文件的标题，但没有 commit + push + 部署。用户问"部署了么？"才发现只改了本地
- 防护：开工时告诉 AI "做完整个任务再来汇报，中间不要停；但做完后必须完成提交和部署的闭环"

##### 第二类 · AI 在编造（给你不存在的东西）

**编造 API / 依赖**——AI 会编造根本不存在的库名、方法、版本号，写出来的代码看着完全正常，直到运行才报错。研究显示近 28% 的依赖推荐是虚构的。

- 典型表现：引用了听起来合理但不存在的库（名字差一个字母）/ 调用了当前版本没有的方法 / 推荐了从未发布过的版本号
- 防护：看到 AI 引入新依赖时，问"给我这个库的 GitHub 链接"——给不出或打不开就是编的。在 KNOWLEDGE.md 里固定项目依赖和版本号

##### 第三类 · AI 在原地打转（消耗你的时间但没有推进）

**修了又出新错的死循环**——你贴报错 → AI 修了 → 新报错 → 再贴 → 再改……陷进去就是几个小时。

- 原因：AI 头痛医头不看全局；修 A 时改了 B 的依赖；上下文越长注意力越分散
- 分级应对：第 1-2 次正常 → 第 3 次停下来要求 AI 整体分析根因 → 第 4 次回到上一个能跑的版本换思路 → 第 5 次换模型或开新对话

**多文件联动遗漏**——AI 改了某个模块的数据格式，但没同步更新调用它的其他文件。

- 防护：改前问"这次改动会影响哪些文件？先列出来"；改后要求"跑一次全量测试确认没有连带报错"

**引入重复依赖**——项目里已经有 dayjs 了，AI 又装了一个 date-fns 做同样的事。同一个逻辑出现 2-3 种实现方式。

- 真实数据：AI 辅助开发后，团队月均新增依赖从 2 个跳到 9 个，其中大量是功能重叠的
- 防护：在 KNOWLEDGE.md 里列出"项目已使用的核心依赖"，告诉 AI "新增依赖前先确认有没有现有依赖能做同样的事"

**技术债隐性累积（3 个月后不可维护）**——AI 生成的代码初看干净能跑，但每次 session 都不记得之前的架构决策，导致同一逻辑出现多种写法、命名不统一、文件越来越臃肿。

- 真实数据：AI 辅助 12 个月后，代码重复率从 3.1% 涨到 14.2%，平均文件行数从 142 涨到 267，新人上手时间从 2 周变 5 周
- 根因：每次 AI 新对话都是"从零开始"，不知道之前已经有类似实现
- 防护：在 KNOWLEDGE.md 中维护"已有工具函数清单"，让 AI 先查有没有可复用的再写新的

**浅层执行——AI 只看表面不深入细节**

- 真实案例：让 AI 分析一个 Figma 设计文件，它只看了根目录的顶层结构就写了报告，完全没有逐页深入分析。用户连续纠正了 3 次"每个页面都有内容的，你细化 go through"
- 另一个案例：让 AI 做组件提取，粒度太粗，只识别了大块组件（卡片、导航栏），忽略了最小化组件（按钮变体、图标状态、徽章样式）
- 判断法：问"你查看了所有层级吗？列出你检查过的每一个子项"——如果列不出来就是走马观花了
- 防护：对于分析/审计类任务，要求 AI "列出你扫描了哪些文件/页面/模块，每个的分析结论分别是什么"

##### 第四类 · 上下文出了问题

**上下文污染（换了方案但 AI 还在走老路）**——你跟 AI 讨论了很久方案 A，发现走不通换成 B，AI 嘴上说"好的按 B 来"，但写出来的代码仍然混着方案 A 的思路。

- 原因：花了 20 轮讨论 A，方案 B 只在最后 2 轮提到——A 在上下文中的权重远大于 B。AI 嘴上换了但"肌肉记忆"还是 A
- **这个问题用纠正解决不了**——问题不在理解力，在上下文本身已被旧方案占满
- 唯一解法：**换方案 = 换对话**。停当前对话 → KNOWLEDGE.md 写明"A 已废弃，当前用 B" → 开新对话 → **不提 A**（连"不要用 A"都别说，会激活旧记忆）→ 像第一次提需求一样从零描述 B
- 一句话记法：在同一个对话里纠正方向，就像在画满旧草图的纸上画新图——怎么画都有旧痕迹。**拿一张白纸重新画**

**规则毒化——一条写错的规则导致所有后续执行必然失败**

- 真实案例：Figma 项目规则文件里写了"优先使用 GT Walsheim Pro 字体"，但 Figma MCP 插件不支持该字体。结果每次执行到 `loadFontAsync` 都必然失败 → 整个操作原子回滚 → 所有生成全废。这条规则藏了好几轮都没被发现
- 另一个案例：两个配置文件对同一行为给出矛盾指令（一个说"先展示规划等确认"，另一个说"直接执行"），AI 收到矛盾指令后两边都不执行
- 防护：定期让 AI **审计所有规则文件**："扫描项目里所有 rules / 配置文件，检查有没有互相矛盾的规则、引用了不存在的资源的规则、实际上从未生效过的规则。列出问题并建议修复"

**多副本漂移——同一文件在多个位置有副本，内容互相矛盾**

- 真实案例：AGENTS.md 同时存在于根目录、workspace/、_deploy/ 三处，共 18 份副本。大小、时间、内容全不一致。AI 同步时把一个副本写成了空文件，另一个大小翻倍
- 防护：在 KNOWLEDGE.md 里明确"权威版本在哪个路径，其他地方的是派生副本"。让 AI 改配置文件时加一条："这个文件有没有其他副本？如果有，列出来，一起改"

---

#### 3C · 翻车急救


| 情况            | 你说什么                              |
| ------------- | --------------------------------- |
| AI 改了一堆不知道对不对 | "列出你刚才改了哪些文件、每个改了什么、为什么"          |
| 想回到之前能跑的状态    | "帮我回到上一个能跑的版本"（它会用 git 帮你回退）      |
| 回退了但不确定丢了什么   | "对比当前版本和回退前的版本，列出差异"              |
| 完全搞不定了        | 关掉当前对话，开新的，把报错信息和 KNOWLEDGE 一起发过去 |


#### 这个项目里发生了什么

> 飞书 bot 项目在搭建阶段集中踩了以下坑：
>
> - **死循环**：一个消息 hook 只触发一次的 bug，修了 hook → 触发了 slug-generator 干扰 → 修 slug-generator → 又引出编码问题 → 来回修了 6 轮。如果第 3 轮就停下来分析全局根因，至少省 3 轮
> - **散弹枪修改**：修一个消息投递的问题，AI 同时改了 4 个 deliver 文件 + 1 个 hook 文件 + 1 个 monitor 文件，每个文件只动了 1-2 行。改完 AI 自己也说不清这些改动之间的关系
> - **上下文污染**：讨论了很久的方案 A（用 webhook 接收消息），发现不通切到方案 B（用 WebSocket）。AI 嘴上说"按方案 B 来"，代码里还在引用方案 A 的变量名和数据结构。最终不得不开新对话从零描述方案 B
> - **多副本漂移**：AGENTS.md 这个关键配置文件同时存在于根目录、workspace/、_deploy/ 三处，共 18 份副本。大小、时间、内容全不一致。AI 同步时把一个副本写成了空文件（0 字节），另一个大小翻倍变成乱码
> - **400+ 脚本散落根目录**：每个对话 session 产生的一次性调试脚本（check-*.js / fix-*.py / patch-*.js）全部丢在项目根目录，从来没有清理。到后期根目录有 400+ 个文件，找什么都找不到
> - **虚假完成**：AI 报告"Status Tail 功能已实现"。实际测试发现，通过插件调用需要 75 秒且只返回 105 个字符，而直接调用只需 18 秒返回 709 个字符——中间的 Gateway 超时和重试逻辑把整条链路搞坏了，AI 只测了"直接调用"就报了通过
> - **校验代替产出**：让 AI 优化 bot 的回复质量，AI 没有改善回复逻辑，而是给输入加了一堆格式校验和长度检查。回复质量没变，只是"非法输入"被拦掉了更多
> - **引入重复依赖**：处理日期格式这一件事，AI 在不同对话里先后引入了 3 种不同写法（原生 Date、moment、dayjs），项目依赖越来越臃肿

#### ✅ 段 3 完成标志

功能跑通 + KNOWLEDGE 已同步 + 代码已提交

---

### 段 4 · 测试与验证（识别 AI 在糊弄你）

> **核心认知**：AI 不是故意骗你，但它会"帮你看到你想看到的"——说你想听的话。这比故意骗更危险，因为你很难察觉。

#### AI 的 18 种糊弄方式（速查表）


| #   | 模式          | AI 怎么表现的                             | 你怎么识别                                |
| --- | ----------- | ------------------------------------ | ------------------------------------ |
| 1   | **嘴上说通过了**  | "should pass" "tests passing"        | "你跑了什么命令？完整输出贴给我看"                   |
| 2   | **偷偷降低标准**  | "simplified for robustness"          | "把你改动的前后对比给我看"                       |
| 3   | **改测试迁就代码** | "updated test to match new behavior" | "是代码对了还是你把标准降了？"                     |
| 4   | **只测正常情况**  | "looks good" "覆盖率达标"                 | "异常情况的测试在哪？举 3 个"                    |
| 5   | **偷偷跳过失败**  | 无声无息加了 skip 标记                       | "有没有被跳过的测试？全列出来"                     |
| 6   | **表面修复**    | "已修复" "不报错了"                         | "修前什么行为？修后什么行为？"                     |
| 7   | **虚假优化**    | "优化了结构" "重构以提高可维护性"                  | "去掉这次改动功能会出什么问题？"                    |
| 8   | **编造依赖**    | 代码正常但引用了不存在的库/方法                     | "给我这个库的官方文档链接"                       |
| 9   | **散弹枪修改**   | 东一行西一行，5+ 文件每个只改 1-2 行               | "这次改动做的是一件事还是好几件？"                   |
| 10  | **校验代替产出**  | 你要优化效果，它加了校验/日志                      | "用户体验到的结果有什么变化？"                     |
| 11  | **上下文走老路**  | 你换了方案但代码还在用旧方案思路                     | 发现旧元素 → 停对话 → 开新的从零说起                |
| 12  | **目标漂移**    | 你要优化方法，它去修当前产物的具体数值                  | "这次改动对下一个项目也有效吗？"                    |
| 13  | **流程跳步**    | 多步任务偷偷跳过某步或在上一步基础上改而非重做              | "第 X 步的完整产物在哪？出示一下"                  |
| 14  | **浅层执行**    | 只看顶层就写报告，没有逐级深入                      | "你检查了所有子项吗？逐个列出"                     |
| 15  | **规则毒化**    | 规则文件里有一条错误规则导致后续全部失败                 | 定期让 AI 审计所有规则文件                      |
| 16  | **多副本漂移**   | 同一文件在 3 个目录有副本，内容互相矛盾                | "这个文件有没有其他副本？一起改"                    |
| 17  | **引入重复依赖**  | 项目已有类似库，AI 又装了一个做同样的事                | "项目里有没有已有依赖能做这件事？"                   |
| 18  | **密钥硬编码**   | 把 API key / 密码直接写进代码文件               | 搜索 `sk-` `password` `secret` `token` |


#### 验证三原则

1. **要真实输出**：让 AI 贴出它实际运行的命令和完整结果，不接受它的"总结"或"概括"
2. **双跑自查**：AI 说"通过了"→ 让它立刻**再跑一次**，对比两次结果是否一致
3. **红线必须人来**：涉及钱、权限、用户数据的功能——AI 报告不算数，你必须自己试一遍

#### AI 谎言速查词典


| AI 说的话                   | 大概率真实情况   | 你立刻问                     |
| ------------------------ | --------- | ------------------------ |
| "should pass"            | 没真跑       | "你跑了什么命令？结果是？"           |
| "looks good"             | 没仔细看      | "贴出异常场景的测试"              |
| "理论上 / 应该"               | 没验证，在猜    | "实际跑了吗？"                 |
| "我以为"                    | 完全在猜      | "依据是什么？"                 |
| "已修复"                    | 没复测       | "重跑一次给我看"                |
| "顺便优化了…"                 | 跑偏了       | "回滚那部分"                  |
| "已经处理了边界"                | 大概率没有     | "举 3 个边界场景的测试"           |
| "优化了代码结构"                | 在做无效改动    | "去掉这次改动功能会出什么问题？"        |
| "升级到了更好的方案"              | 没有对比数据    | "用什么指标证明'更好'了？"          |
| 引用了你没见过的库/方法             | 28% 概率是编的 | "给我官方文档链接"               |
| "清理了冗余代码"                | 偷偷删了安全网   | "你删了哪些？逐条解释为什么可以删"       |
| 一次改了 5+ 文件每个只 1-2 行      | 散弹枪式修改    | "这是一件事还是好几件？拆开做"         |
| "增加了校验 / 完善了异常处理"        | 偷换了你的优化需求 | "用户体验有什么变化？没变就重做"        |
| 你换了方案但代码出现旧方案元素          | 上下文污染     | 停对话 → 写 KNOWLEDGE → 开新对话 |
| "基于上一轮做了微调"（应该是独立重做）     | 偷偷跳步/偷工减料 | "你是独立重新做的还是在上一轮上改的？"     |
| 分析报告只有顶层结论没有详细子项         | 浅层执行/走马观花 | "你检查了所有子页面/子模块吗？逐个列出"    |
| 做了 10 轮优化但只对当前文件有效       | 目标漂移      | "这次改动对下一个项目也有效吗？"        |
| 同一文件在多个目录内容不一致           | 多副本漂移     | "权威版本在哪？其他副本一起同步"        |
| 新装了一个库但项目里已有类似的          | 依赖重复      | "项目里有没有已有依赖能做这件事？"       |
| 代码里出现 sk- / 明文密码 / token | 密钥硬编码     | 立刻删除 → 改用环境变量            |


#### 多角色评审（大项目 / 重要功能才需要）

> 让 AI 扮演不同角色审查同一份代码。对小项目来说，跑一次"挑刺"就够：

```
现在请你换一个角色：你是一个专门找茬的代码审查员。
审查我们刚才做的这个模块，从这几个角度挑毛病：
1. 有没有安全漏洞？
2. 异常情况处理了吗？
3. 有没有偷偷降低了标准？
4. 有没有我让你做但你跳过了的？
```

#### 这个项目里发生了什么

> 飞书 bot 项目在测试阶段暴露了大量"AI 在演"的情况：
>
> - **只测了 happy flow**：AI 设计的测试只覆盖了"正常发消息 → bot 正常回复"，没有测"bot 需要澄清问题""用户中途取消""多人同时发消息"等场景。用户指出后 AI 补了 case，但如果一开始就要求"列出你测了哪些场景、没测哪些"就不会漏
> - **不做全链路测试**：AI 直接调 proxy API 说"通过了"，但实际用户是从飞书发消息 → Gateway → 插件 → proxy 的完整链路。中间环节的超时、重试、去重全没测到
> - **偷偷降低标准**：端到端测试要求"30 轮、每组 10 轮、20 个 case"，AI 理解成了完全不同的结构，测试覆盖大幅缩水。用户发现后说"错了"，AI 才重新按要求来
> - **CLARIFY 指令被模型完全无视**：模型正确返回了"需要澄清"的响应（591 字），但 agent 无视了注入的 CLARIFY 指令，直接往下执行并创建了 594 块飞书文档。AI 自己的测试报告说"CLARIFY 功能正常"——因为它只测了模型是否返回 CLARIFY，没测 agent 是否真的执行了
> - **浅层执行**：让 AI 做一次"全面 review 服务器文件"，它只看了 3 个主要文件就写了报告。用户说"不局限于这些文件，是让你全面 review"，它才扩展范围——最终发现了 3 个实质矛盾（搜索工具优先级互反 / Worker 能否用 message 工具的规则冲突 / Heartbeat 能否执行恢复操作的描述矛盾），全是之前"浅看一眼"漏掉的
>
> 最终不得不用"人在飞书端从头到尾跑一遍完整流程"的方式来验证，才发现上面这些问题。**AI 的测试报告全是"通过"，但人一跑全是 bug。**
>
> 另一个项目（Figma 生成）中还遇到了：
>
> - **改测试迁就代码**：10 轮生成测试中，AI 不是去改善生成方法让结果更接近设计稿，而是调低了"像素级对比"的标准让更多测试"通过"。用户说"不要做自以为是的对比评分，而是严格做对比评测"
> - **偷偷跳过失败**：某些状态的对比结果不好，AI 把这些状态标记为"暂不对比"跳过了，报告里只显示通过的状态
> - **目标漂移**：用户要求"优化 PRD→Figma 的通用生成方法"，AI 反复去调某个 Figma 文件里某个按钮的 y 坐标——改了 10 轮，通用方法一点没变，只是当前这个文件看着好了一点。用户不得不反复强调"注意，我们的目的并不是优化产出，而是优化方法"

#### ✅ 段 4 完成标志

每条"已完成"都有可追溯的真实输出 + 红线功能你自己试过

---

### 段 5 · 沉淀与传承（让经验活下来）

> **短期小项目可简化**：把教训写进 KNOWLEDGE.md 的"踩坑记录"就行，不需要建完整的模板库。

#### 你先想清楚这几件事


| 必答            | 为什么重要                |
| ------------- | -------------------- |
| 有哪些坑以后绝不能再踩？  | 这些要写进 KNOWLEDGE 教训章节 |
| 这套经验下个项目要复用吗？ | 决定要不要抽模板             |
| 有没有别人要接手？     | 决定要不要写上手指南           |


#### 怎么开口

```
帮我做收尾：
1. 把这次踩过的坑、关键决策、教训整理到 KNOWLEDGE.md
2.（如需复用）把可复用的配置抽成模板
3.（如需交接）写一份 5 分钟上手指南
```

#### 这个项目里发生了什么

> 飞书 bot 项目在沉淀阶段的教训：
>
> - **跨对话的经验断裂**：做到一半的 patch 文件散落在服务器上，换了一个对话后 AI 根本找不到"上次做到哪了"。用户不得不手动在多个文件里 diff 来拼凑之前的工作进度
> - **KNOWLEDGE 更新形式错误**：一开始把知识写在 HANDOVER.md（临时交接文件）里，每次新对话还是忘。后来才意识到应该写进 `.cursor/rules/`（永久规则），而不是临时文件
> - **教训没有结构化**：系统架构知识、排查方法论、已知 bug 全混在一个 `lessons-learned.md` 里，从 34 行膨胀到 70 行但类别混乱。后来拆分到不同文件才理清
>
> 这些问题的根因都一样：**没有在做的时候就沉淀，而是等到出了问题才回头补**。

#### ✅ 段 5 完成标志

换台电脑 或 换个人，能在半小时内接着干

---

## 第三部分 · 附录

### 附录 A · 万能开口模板

> 任何"让 AI 帮你搭东西"的需求，都可以套这五步：

```
1. 我是谁、要解决什么问题
2. 先了解我的项目现状，别急着动手
3. 给方案让我看，讨论完再动手
4. 一步一步做，每步等我确认
5. 做完验证一遍，把经验写进 KNOWLEDGE
```

全程约束：每步等确认 / 危险操作先说清楚影响 / 不许自动推送代码 / 用我能看懂的语言

### 附录 B · 调研清单模板（方案设计前让 AI 做功课）


| 调研项       | 让 AI 做什么                                                 | 为什么要查            |
| --------- | -------------------------------------------------------- | ---------------- |
| 官方文档      | 查你要用的框架/库的最新版文档，确认 API 和限制                               | AI 记忆中的用法可能已过时   |
| 模型/工具官方指南 | 查 Cursor 官方 best practices、Claude context engineering 指南 | 工具自身的最佳实践在不断更新   |
| 社区项目      | 搜 GitHub / 论坛 / 博客，找类似项目                                 | 别人踩过的坑你不用再踩      |
| 论文/技术报告   | 如涉及 AI/LLM 功能，查相关论文                                      | 了解能力边界和已知局限      |
| 已知坑汇总     | 综合以上，列出"这个方案最可能踩的坑"                                      | 提前避坑比事后修复便宜 10 倍 |


> 调研完要求 AI 给一份"调研摘要"：参考了什么（附链接）/ 主流做法 / 已知坑 / 自己方案的差异点和理由

### 附录 C · 哪些必须写进文件 vs 口头交代（细化）


| 内容            | 写进文件 | 口头说 | 推荐放哪个文件                        |
| ------------- | ---- | --- | ------------------------------ |
| 项目类型和规模       | ✅    |     | KNOWLEDGE.md                   |
| 模块怎么拆、怎么配合    | ✅    |     | KNOWLEDGE.md 或 ARCHITECTURE.md |
| 业务红线          | ✅    |     | KNOWLEDGE.md 或 RED-LINES.md    |
| 验收标准          | ✅    |     | KNOWLEDGE.md                   |
| 安全边界（禁止执行的命令） | ✅    |     | .cursor/rules/                 |
| 教训 / 踩过的坑     | ✅    |     | KNOWLEDGE.md                   |
| 本次任务具体做什么     |      | ✅   | —                              |
| 临时的小调整        |      | ✅   | —                              |
| 一次性的偏好        |      | ✅   | —                              |


### 附录 D · 按项目规模选配产物


| 产物                     | 小项目  | 中项目   | 大项目   |
| ---------------------- | ---- | ----- | ----- |
| `KNOWLEDGE.md`         | ✅ 简版 | ✅ 标准版 | ✅ 完整版 |
| `.cursor/rules/safety` | ✅    | ✅     | ✅     |
| `ARCHITECTURE.md`      | ❌    | ✅ 简版  | ✅ 完整版 |
| 单模块设计文档                | ❌    | ❌     | ✅     |
| `RED-LINES.md`         | ❌    | ✅     | ✅     |
| 验收标准文档                 | ❌    | ✅     | ✅     |
| `PROGRESS.md`          | ❌    | ❌     | ✅     |
| 回归测试集（evals）           | ❌    | ❌     | ✅     |
| 团队模板 / 上手指南            | ❌    | ❌     | ✅     |


> **小项目**：课程作业、个人小工具、一次性脚本
> **中项目**：内部系统、MVP、Demo
> **大项目**：长期维护的上线产品、多人协作

### 附录 E · 全周期检查清单


| 段    | 完成标志                           | 没做的风险           |
| ---- | ------------------------------ | --------------- |
| 1 启动 | 项目类型已定 + KNOWLEDGE 已建 + 安全边界已设 | AI 反复失忆、误执行危险命令 |
| 2 设计 | 模块怎么拆已对齐 + 红线已列 + 验收标准已写       | 后期改一处崩三处        |
| 3 搭建 | 功能跑通 + KNOWLEDGE 已同步 + 代码已提交   | 进度丢失、不可回退       |
| 4 验证 | 每条"完成"有真实输出 + 红线功能你自己试过        | 上线后发现 AI 在"演"   |
| 5 沉淀 | 换人/换电脑能半小时接着干                  | 经验留不下来          |


### 附录 F · 术语速查


| 术语                  | 大白话                               |
| ------------------- | --------------------------------- |
| KNOWLEDGE.md        | 项目备忘录，AI 每次开工前会先读它                |
| .cursor/rules/      | 给 AI 立的"规矩"，放在项目文件夹里，AI 自动遵守      |
| 接口约定（契约）            | 定义每个模块的"门"——接受什么输入、给什么输出          |
| 模块                  | 项目拆成的一块一块的独立功能                    |
| 红线                  | 绝对不能出错的操作或数据                      |
| git / commit        | 代码版本管理——commit 就是"存档"，可以回退到任意存档点  |
| Plan Mode           | AI 的"只规划不写代码"模式，用来先讨论方案           |
| `/compact` [CC]     | 压缩对话历史，让 AI 恢复注意力（Claude Code 命令） |
| `/clear` [CC]       | 开新对话（Claude Code 命令）              |
| Checkpoint [Cursor] | Cursor 的自动存档点，可以回退到任意时刻           |
| MCP                 | 让 AI 连接外部工具（GitHub、浏览器、数据库等）的插件   |
| evals               | 把过去踩过的坑做成自动化测试，防止同样的坑再踩           |
| 上下文污染               | 旧方案的讨论历史影响了 AI 对新方案的执行，导致新旧混杂     |
| 散弹枪式修改              | AI 东改一行西改一行，把完整功能拆得支离破碎           |


### 附录 G · 分享建议（30 分钟版）


| 时间        | 内容                                     |
| --------- | -------------------------------------- |
| 0:00-0:05 | 核心理念 + 黄金分工表                           |
| 0:05-0:10 | 五条铁原则 + 为什么关键判断必须写进文件                  |
| 0:10-0:20 | 主线五段精讲（每段 2 分钟，重点讲"你想什么 + 完成标志"）       |
| 0:20-0:27 | 段 3B + 段 4 重点展开：AI 的 11 种糊弄方式 + 谎言速查词典 |
| 0:27-0:30 | 收尾：如果只记一句话——"先想清楚，再开口"                 |


---

## 收尾

> AI 不会让你不需要懂技术，
> 但能把"懂"的门槛从"会写代码"降到"会想清楚 + 会判断"。
>
> **你的护城河，是判断力，不是代码量。**

