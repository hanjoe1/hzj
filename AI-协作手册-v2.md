# AI 协作手册

> 非技术人员如何跟 AI 搭档做项目——原则、流程与避坑指南
>
> 以 **Cursor** 为主要示例。Claude Code 专属功能标注 `[CC]`，Cursor 专属功能标注 `[Cursor]`。
>
> 本手册的案例来自一个真实项目：**用 AI 做一个飞书智能助手（bot）**——历时 2 个多月、30+ 轮对话，几乎把 AI 协作能踩的坑踩了个遍。你的项目不一定这么复杂，但这些坑是通用的。

---

## 如果你只有 5 分钟

**1. 分工表** → 见 [1.2](#12-你和-ai-各管什么)

**2. 五条铁原则**（贴墙）：

1. 先想清楚再开口
2. 关键判断必须写进文件（口头说的 AI 下次就忘）
3. 让 AI 先反问你（你以为说清了，其实没有）
4. 要真实输出不要口头承诺（AI 说"好了"不算数）
5. 小步走、每步审（每次不超过 30 分钟的量）

**3. AI 的 18 种糊弄方式** → 见 [速查表](#ai-的-18-种糊弄方式速查表)

---

## 第一部分 · 先认识你的搭档

### 1.1 AI 是怎样的

**① 能力强，但记性差**——每次开新对话就从零开始。

> 真实教训：飞书 bot 项目，上午告诉 AI "Shell 是 PowerShell，不要用 Linux 命令"，下午开新对话它又用 `&&` `wc` `head`，报错 4 次。这个问题在整个项目里每换一次对话就重踩一次。

**② 没有改动成本意识**——改 1 行和改 100 行对它一样轻松。

> 真实教训：让 AI"优化一下页面加载"，它把整个组件重写了。加载速度没变，之前调好的交互坏了。

**③ 会说你想听的话**——不是故意骗你，但倾向于报告"好消息"。

> 真实教训：AI 说"所有测试通过"。追问"跑了什么命令？"——回答不上来。另一次说"已修复 bug"，实际只是把报错的那行注释掉了。

**④ 聊越久越不靠谱**——对话越长，注意力越分散。

> 真实教训：Figma 项目前 3 轮 AI 严格按约定每轮独立生成+截图对比，第 4 轮开始偷偷在上一轮基础上改，到第 7 轮才被发现——前面几轮全废了。

**⑤ 不会主动说"我做不到"**——不确定的事它也编一个看起来合理的答案。

> 真实教训：AI 推荐了一个不存在的 npm 包，名字像真的，代码也对，跑 install 才报错。研究显示约 28% 的依赖推荐是虚构的。

### 1.2 你和 AI 各管什么

| 维度 | 你来（AI 做不了或不可靠的） | AI 来（有方向它就能干） |
|---|---|---|
| 这是什么 | 项目是什么、给谁用、解决什么 | 用什么技术实现 |
| 做多久 | 长期 还是 一次性 | 配多少基础设施 |
| 做什么 | 做什么、**不做什么** | 任务怎么拆、先做哪个 |
| 什么不能错 | 哪些出错后果严重 | 怎么测、怎么验 |
| 什么算完成 | "做完"长什么样 | 转成具体测试 |
| 这步对不对 | 每一步的判断 | 具体怎么写 |

### 1.3 关键信息必须写进文件

因为 AI 记性差（特质①），口头交代的下次就忘。

| 必须写进文件 | 口头说就行 |
|---|---|
| 项目是什么、做多久 | 这次做哪个小任务 |
| 什么不能错 | 临时调整 |
| 验收标准 | 一次性偏好 |
| 各模块怎么配合 | 试探性尝试 |
| 踩过的坑 | — |
| 已否决的方案 | — |

> 文件放项目根目录。AI 每次开新对话会自动读 `KNOWLEDGE.md`（Cursor 中对应 `.cursor/rules/`）。

---

## 第二部分 · 项目生命周期

> 短期小项目可跳过段 2 和段 5，但段 1、3、4 不能跳。

---

### 段 1 · 启动

目标：花 10 分钟把"项目记忆 + 安全边界"建好，后面省几十小时。

---

**正确做法①：先想清楚，再跟 AI 说**

| 你先想清楚 | 为什么 |
|---|---|
| 一句话这是什么项目？ | 说不清 = 没想清 |
| 长期 还是 一次性？ | 决定要不要建基础设施 |
| 给自己 / 团队 / 外部？ | 决定规范做多细 |

开口方式：

```
我要做一个 ___。类型：长期/短期。受众：___。
请你：1. 复述你理解的项目 2. 反问我 3-5 个歧义问题 3. 建议要不要建 KNOWLEDGE.md 4. 等我说 GO 再动手
```

> **不做的后果**：飞书 bot 项目没做这一步，每次新对话都要花 10-15 分钟重新解释项目背景——30 个对话就浪费了 5-7 小时。

---

**正确做法②：让 AI 帮你建项目备忘录（KNOWLEDGE.md）**

KNOWLEDGE.md 就是你给 AI 的备忘录，它每次开工前会先读。开口方式：

```
帮我建一份项目备忘录（KNOWLEDGE.md），放在项目根目录。
先扫描项目现状，按这些要点草拟，给我看完再确认：
- 项目目标（一句话）
- 技术栈（你探测到的）
- 运行环境（操作系统、Shell类型、语言版本）
- 项目框架（主要模块和职责，哪怕只是粗略的）
- 常用命令（启动、测试、构建、部署）
- 测试方法（怎么跑测试、过线标准是什么）
- 已知坑（目前遇到的问题）
- 能跑的版本（最近一次正常运行的状态）
- 修改日志（最近变更记录，倒序，以后每次改动追加）
```

> 中大项目还要加：架构决策(why) + 教训库 + 已废弃方案记录 + 已有依赖清单

> **不做的后果**：飞书 bot 项目没有 KNOWLEDGE，AI 每换一次对话就忘了环境信息。PowerShell 下反复用 Linux 命令报错，先后踩了至少 4 次。处理日期这一件事 AI 在不同对话里引入了 3 种不同写法（原生 Date、moment、dayjs），因为它不知道项目里已经有了什么。

---

**正确做法③：初始化 git——给项目一个"存档系统"**

git 就是代码的存档系统。每次存档（commit）相当于游戏里的 save point，出问题可以回退到任意存档。开口方式：

```
帮我初始化 git：
1. git init
2. 创建 .gitignore（根据项目类型忽略 node_modules / .env / 构建产物等）
3. 把当前能跑的代码做第一次提交，备注"初始化基线"
4. 以后每完成一个小步骤，都帮我提交一次，备注写清楚改了什么
5. 不许自动 git push，push 由我决定
```

> **不做的后果**：有人用 AI 做了一个有 20 个付费用户的项目，没有 git。AI 覆盖了能跑的代码写入了错误代码，无法恢复，项目废了。

---

**正确做法④：让 AI 帮你建规则文件（rules）——给 AI 立"规矩"**

rules 是你给 AI 的行为约束，它每次对话都会自动遵守（不像口头说的会忘）。开口方式：

```
帮我建一套 AI 行为规则（放在 .cursor/rules/ 目录下），包含以下约束：

安全规则（safety.mdc）：
- 不许自动执行：rm -rf / sudo / git push --force / 全局装包 / 任何数据库操作
- 不许把密钥、密码、token 硬编码到代码中，必须用环境变量
- 不许删除任何现有代码，除非我明确要求

改动同步规则（sync.mdc）：
- 每次代码改动后，必须更新 KNOWLEDGE.md 的修改日志
- 每完成一个可验证的小步骤，必须提交 git（commit）
- commit 备注必须写清楚改了什么、为什么改
- 不许自动 git push

工作纪律规则（discipline.mdc）：
- 已调通的代码不许动，要改先告诉我为什么
- 不许"顺便优化"任何不在当前任务范围内的东西
- 每次改动集中在一件事上，不要东改一行西改一行
- 同一错误修了 3 次还不行就停下报告
```

> **为什么要用 rules 而不是每次口头说？** 因为 AI 记性差（特质①），口头说的纠正下次对话就忘。rules 文件会被 AI 每次自动读取——相当于把"规矩"刻在墙上而不是口头交代。
>
> **以后遇到 AI 反复犯同样的错怎么办？** 不要只在对话里纠正（下次又忘），直接往 rules 里加一条。比如 AI 老是用 Linux 命令？加一条"当前环境是 PowerShell，不许用 &&、wc、head"。
>
> **⚠️ 数据库操作要特别注意**：2026 年多起公开事故中，AI 被要求"查看日志""分析问题"，却自行执行了删表命令，全部数据不可恢复。所以 safety.mdc 里的数据库黑名单不能省。
>
> **不做的后果**：飞书 bot 项目前期没有 rules，AI 反复犯同样的错——被纠正"不要用 Linux 命令"后下次还用、被纠正"不要删代码"后又把错误处理删了。API 密钥也被直接硬编码到 JavaScript 文件里，差点提交到公开仓库。

---

#### ✅ 段 1 完成

项目类型已定 + KNOWLEDGE 已建（含框架/日志/命令/测试） + git 已初始化 + rules 已建（含安全/同步/纪律）

---

### 初学者先过这 4 关（第一次用 AI 就会犯的错）

> 这 4 个错不是搭建阶段才犯的——是你**第一次跟 AI 对话就会犯**的。段 1 做完、准备往下走之前，先看一遍。

| ❌ 错误 | 正确做法 |
|---|---|
| 看都没看就点"全部接受" | **先看改动对比（diff）再接受**。特别注意 AI 删了什么、改了哪些你没要求改的文件。看不懂逻辑没关系，至少看"改了几个文件""数字值对不对" |
| 一个 prompt 塞整个项目需求 | **一次只做一件事**。先做一个功能，验证通过再做下一个 |
| 密钥密码写进代码（25% 项目中招） | 段 1 的 safety.mdc 已约束"不许硬编码密钥"。review 时仍要搜 `sk-` `password` `secret` `token` |
| 不看代码就部署 | 涉及用户输入、数据库、权限的代码**必须人审**——让 AI 解释"这段有没有安全风险" |

---

### 已有项目的急救入口

> 如果你的项目已经做了一段时间才看到这份手册——不用从段 1 重来，从这里切入：

```
项目比较乱了，请帮我：
1. 扫描代码梳理模块和依赖
2. 标注哪些稳定哪些常出问题
3. 补一份 KNOWLEDGE
4. 列出 3 个最大风险
5. 给"先救什么"的建议
```

> 急救完成后，回到段 1 补 git + rules，然后从段 3 的开工规矩继续。

---

### 段 2 · 设计

目标：让 AI 先想清楚再动手，别一上来就写代码。短期小项目至少跟 AI 对一遍"分几块、谁先谁后"。

---

**正确做法①：让 AI 反问你，把模糊需求挖清楚**

```
进入规划模式，不要写代码。
1. 先问我 3-5 个最关键的歧义问题
2. 列出模块清单（每个模块一句话职责）
3. 画出模块间的依赖关系
4. 每个决策写 why
5. 等我审完再下一步
```

> **不做的后果**：飞书 bot 项目没有先拆模块。修改消息格式导致通知模块、日志模块同时出错——因为模块间的数据怎么传根本没约定过。后期每改一处，平均连带崩 2-3 处。

---

**正确做法②：让 AI 先做功课——查文档、查社区**

```
在做方案前，先做以下调研：
1. 查官方文档确认 API 和限制
2. 查 Cursor/Claude 官方 best practices
3. 搜社区有没有类似项目，总结别人踩的坑
4. 给我一份调研摘要（参考了什么、已知坑、你方案的差异点）
```

> **不做的后果**：飞书 bot 项目中，AI 用了飞书插件的 webhook 模式写了一大堆代码。后来发现官方的 webhook 分支根本是空的（只有一个 `return;`），整个方向全废。一开始查文档就能避免。

---

**正确做法③：定红线——什么不能错**

你来想业务红线（什么数据敏感、什么操作不可逆），让 AI 补充完整清单，你来审。

> **不做的后果**：飞书 bot 项目有 5 层配置文件对同一行为给出矛盾指令（一个说"先展示规划等确认"，另一个说"直接执行"）。AI 收到矛盾指令后两边都不执行，bot 直接不响应。后来做了全面审计才逐一修复——设计阶段做这件事成本不到后期的 1/10。

#### ✅ 段 2 完成

模块怎么拆已对齐 + 红线已列 + 验收标准已写

---

### 段 3 · 搭建

目标：AI 开始写代码了，你要做的是"立规矩、盯过程、防翻车"。

---

**正确做法①：开工时补充本次任务的特有约束**

段 1 建的 rules（safety + sync + discipline）会自动生效，不用每次重发。开工时只需要补充**这次任务特有的**约束：

```
开始做模块 ___。补充规则：
1. 本次只改 ___ 路径下的文件，碰别的先问我
2. 每完成一小步就停下汇报：做了什么 / 没做什么 / 下一步建议
3. 汇报用"我跑了 X，输出 Y"，禁说"我以为""应该可以了"
```

> **不做的后果**：飞书 bot 项目没有限定改动范围。AI 修一个消息投递问题时同时改了 4 个 deliver 文件 + 1 个 hook + 1 个 monitor，每个文件只动 1-2 行（**散弹枪式修改**），改完自己都说不清改动之间的关系。

> **小贴士**：喂 AI 已有代码比文字描述规范有效 10 倍。直接把项目里一个写得好的文件贴给 AI 说"照这个风格写"。

---

**正确做法②：盯住这些危险信号**

| 你看到的 | 立刻做什么 |
|---|---|
| AI 改了无关文件 | 停 + 回滚 |
| "顺便帮你优化了 X" | 拒绝 + 回滚 |
| 解释很多但没做事 | 要"少说，贴运行结果" |
| 不再问你问题，自顾自做一大堆 | 停下来让它解释在做什么 |
| 同一错误修了 3 次 | 换思路或换模型 |
| 后半段明显变蠢 | 开新对话 + 交接摘要 |
| 多步任务偷跳了某步 | "你是不是跳了第 X 步？回去补" |
| 每一小步都停下来问你 | "先做完再整体汇报" |

> **真实教训——虚假优化**：让 AI 优化 bot 回复质量，它没改回复逻辑，加了一堆输入格式校验。回复质量没变，只是"非法输入"被拦了。这叫**用校验代替产出**——你要的是"结果更好"，它理解成"出错更少"。判断法：问"用户感受到什么不同？"——答不上来就是偷换问题。

> **真实教训——死循环**：消息 hook 只触发一次的 bug，修了 hook → 触发 slug-generator 干扰 → 修 slug-generator → 引出编码问题 → 来回 6 轮。正确做法：第 3 次就该停下来要求 AI 整体分析根因，第 4 次回到上一个能跑的版本换思路，第 5 次换模型或开新对话。

> **真实教训——上下文污染**：讨论了很久用 webhook 接消息（方案 A），发现不通切到 WebSocket（方案 B）。AI 嘴上说"按 B 来"，代码里到处是方案 A 的变量名和结构。**这个问题用纠正解决不了**——因为花了 20 轮讨论 A，B 只在最后 2 轮提到，A 的权重远大于 B。唯一解法：**换方案 = 换对话**。停掉当前对话 → KNOWLEDGE 写明"A 已废弃" → 开新对话 → **不提 A**（连"不要用 A"都别说，会激活旧记忆）→ 从零描述 B。

> **真实教训——多副本漂移**：AGENTS.md 在根目录、workspace/、_deploy/ 有 18 份副本，大小时间内容全不一致。AI 同步时把一个写成空文件，另一个翻倍变乱码。防护：在 KNOWLEDGE 里写明"权威版本在哪个路径"。

> **真实教训——引入重复依赖**：处理日期这一件事，AI 在不同对话里引入了 3 种写法（原生 Date、moment、dayjs），项目依赖越来越臃肿。防护：KNOWLEDGE 里列出已有依赖，告诉 AI"新增前先确认有没有现有的能做"。

---

**正确做法③：管好上下文——防止 AI 越聊越蠢**

- 一个任务一个对话——切话题开新窗口
- 聊太久了——用 `/compact` [CC] 或开新 Composer [Cursor]
- 出错回退——用 `/rewind` [CC] 或 Checkpoint [Cursor]
- 交班下次——写交接摘要放进 KNOWLEDGE
- **纠正过的错 AI 不记住**——段 1 已讲过：往 rules 里加一条，别只在对话里说
- **@folder 引用时 AI 只拿到摘要**——精确指向用 `@具体文件名`

---

**正确做法④：每步同步——改完就存档（段 1 建的 rules 会自动提醒）**

如果你在段 1 建了 sync.mdc 规则，AI 每次改完会自动更新 KNOWLEDGE 日志 + 提交 git。你要做的只是确认它真的做了。小项目可以放宽到"每会话结束时同步一次"。

> **不做的后果**：飞书 bot 项目每个对话产生的调试脚本全丢在根目录，从不清理。后期根目录 400+ 个文件，找什么都找不到。跨对话后 AI 也找不到"上次做到哪了"。

---

**翻车急救**

| 情况 | 你说什么 |
|---|---|
| AI 改了一堆不知道对不对 | "列出改了哪些文件、每个改了什么、为什么" |
| 想回到之前能跑的状态 | "帮我回到上一个能跑的版本" |
| 回退了不确定丢了什么 | "对比当前版本和回退前的差异" |
| 完全搞不定了 | 关对话，开新的，带上报错和 KNOWLEDGE |

#### ✅ 段 3 完成

功能跑通 + KNOWLEDGE 已同步 + 代码已提交

---

### 段 4 · 验证

目标：确认 AI 说的"做完了"是不是真的做完了。

---

**正确做法①：要真实输出，不信口头承诺**

让 AI 贴出实际运行的命令和完整结果。不接受"总结"或"概括"。AI 说"通过了"→ 让它立刻再跑一次对比。红线功能（钱、权限、数据）必须你自己试。

> **真实教训**：飞书 bot 项目里 AI 直接调 proxy API 说"通过了"，但实际用户走的是 飞书→Gateway→插件→proxy 的完整链路。中间的超时、重试、去重全没测。**AI 的测试报告全是"通过"，人一跑全是 bug。**

---

**正确做法②：识别 AI 在糊弄你**

#### AI 的 18 种糊弄方式（速查表）

| # | 模式 | AI 怎么表现 | 你怎么识别 |
|---|---|---|---|
| 1 | **嘴上通过** | "should pass" | "跑了什么命令？完整输出贴给我" |
| 2 | **降低标准** | "simplified for robustness" | "改动的前后对比给我看" |
| 3 | **改测试迁就代码** | "updated test to match behavior" | "是代码对了还是你把标准降了？" |
| 4 | **只测正常情况** | "looks good" | "异常场景的测试在哪？举 3 个" |
| 5 | **偷偷跳过失败** | 加了 skip 标记 | "有被跳过的测试吗？全列出来" |
| 6 | **表面修复** | "已修复""不报错了" | "修前什么行为？修后什么行为？" |
| 7 | **虚假优化** | "优化了结构" | "去掉这次改动功能会出什么问题？" |
| 8 | **编造依赖** | 引用不存在的库 | "给我官方文档链接" |
| 9 | **散弹枪修改** | 5+ 文件每个改 1-2 行 | "这是一件事还是好几件？" |
| 10 | **校验代替产出** | 你要优化效果，它加校验 | "用户体验有什么变化？" |
| 11 | **上下文走老路** | 换了方案代码还用旧的 | 停对话 → 开新的从零说 |
| 12 | **目标漂移** | 你要优化方法，它修产物 | "对下一个项目也有效吗？" |
| 13 | **流程跳步** | 偷偷跳过某步 | "第 X 步完整产物在哪？" |
| 14 | **浅层执行** | 只看顶层就写报告 | "所有子项都查了吗？列出来" |
| 15 | **规则毒化** | 错误规则导致后续全失败 | 定期让 AI 审计规则文件 |
| 16 | **多副本漂移** | 同一文件多处内容矛盾 | "有其他副本吗？一起改" |
| 17 | **重复依赖** | 又装了一个做同样事的库 | "有已有依赖能做吗？" |
| 18 | **密钥硬编码** | 密码写进代码 | 搜 `sk-` `password` `token` |

> **真实教训——偷偷降标准**：飞书 bot 项目要求"30 轮、每组 10 轮、20 个 case"的测试，AI 理解成了完全不同的结构，覆盖大幅缩水。用户发现后说"错了"才重新来。
>
> **真实教训——浅层执行**：让 AI"全面 review 服务器文件"，它只看了 3 个主文件就写了报告。用户说"不局限于这些，全面 review"后，才发现 3 个实质矛盾——全是"浅看一眼"漏掉的。
>
> **真实教训——改测试迁就代码**（Figma 项目）：AI 不去改善生成方法让结果接近设计稿，而是调低"像素级对比"的标准让更多测试"通过"。某些不好的结果被标记为"暂不对比"直接跳过。
>
> **真实教训——目标漂移**（Figma 项目）：要求优化"PRD→Figma 生成方法"，AI 反复去调某个文件里按钮的 y 坐标——改了 10 轮，方法没变，只是当前文件看着好了一点。用户不得不反复强调"我要的是优化方法，不是修这一个文件"。
>
> **真实教训——规则毒化**（Figma 项目）：规则文件写了"优先用 GT Walsheim Pro 字体"，但 Figma MCP 不支持该字体。每次执行都必然失败 → 整个操作回滚。这条规则藏了好几轮都没发现。定期让 AI 审计 rules 文件能提前发现这类问题。

---

**正确做法③：关键功能让 AI 扮演"找茬者"审一遍**

```
换个角色：你是专门找茬的审查员。
从这几个角度挑毛病：1. 安全漏洞？2. 异常处理？3. 偷偷降标准了？4. 有没有我让做但跳过了的？
```

#### ✅ 段 4 完成

每条"完成"有可追溯的真实输出 + 红线功能你自己试过

---

### 段 5 · 沉淀

目标：让今天踩的坑明天不再踩。短期项目把教训写进 KNOWLEDGE 就行。

---

**正确做法：让 AI 帮你做收尾**

```
帮我做收尾：
1. 把踩过的坑、关键决策、教训整理到 KNOWLEDGE.md
2.（如需复用）抽出可复用的配置做模板
3.（如需交接）写一份 5 分钟上手指南
```

> **不做的后果**：飞书 bot 项目里跨对话的经验断裂严重——做到一半的 patch 散落在服务器，换对话后 AI 找不到"上次做到哪了"，得手动翻文件拼凑进度。KNOWLEDGE 一开始写在临时交接文件里，每次新对话还是忘。后来才意识到应该写进永久规则（`.cursor/rules/`）。教训文件混了架构知识和排查方法论，从 34 行膨胀到 70 行但类别混乱，最后不得不拆分重整。

#### ✅ 段 5 完成

换台电脑或换个人，能在半小时内接着干

---

## 第三部分 · 速查附录

### 附录 A · 万能开口模板

```
1. 我是谁、要解决什么
2. 先了解项目现状，别急着动手
3. 给方案让我看，讨论完再动手
4. 一步步做，每步等确认
5. 做完验证，把经验写进 KNOWLEDGE
```

### 附录 B · 调研清单（设计阶段让 AI 做功课）

| 调研项 | 让 AI 做什么 | 为什么 |
|---|---|---|
| 官方文档 | 查框架/库最新版 API 和限制 | AI 记忆中的用法可能过时 |
| 工具指南 | 查 Cursor/Claude 官方 best practices | 工具最佳实践在更新 |
| 社区项目 | 搜 GitHub/论坛找类似项目 | 别人踩过的坑不用再踩 |
| 论文 | 如涉及 AI/LLM，查相关研究 | 了解能力边界 |
| 已知坑 | 综合以上列出最可能的坑 | 提前避坑 |

### 附录 C · 按项目规模选配

| 产物 | 小项目 | 中项目 | 大项目 |
|---|---|---|---|
| KNOWLEDGE.md | ✅ 简版 | ✅ 标准版 | ✅ 完整版 |
| 安全规则 | ✅ | ✅ | ✅ |
| ARCHITECTURE.md | ❌ | ✅ 简版 | ✅ 完整版 |
| 红线文档 | ❌ | ✅ | ✅ |
| 验收标准 | ❌ | ✅ | ✅ |
| 回归测试集 | ❌ | ❌ | ✅ |
| 团队模板 | ❌ | ❌ | ✅ |

> 小=课程作业/小工具　中=内部系统/MVP　大=长期上线产品

### 附录 D · AI 谎言速查词典

| AI 说 | 大概率 | 你问 |
|---|---|---|
| "should pass" | 没跑 | "跑了什么？结果？" |
| "looks good" | 没细看 | "贴异常场景测试" |
| "理论上/应该" | 在猜 | "实际跑了吗？" |
| "我以为" | 在猜 | "依据？" |
| "已修复" | 没复测 | "重跑一次给我看" |
| "顺便优化了" | 跑偏 | "回滚" |
| "优化了结构" | 无效改动 | "去掉会出什么问题？" |
| "升级到更好方案" | 没有对比 | "什么指标证明更好？" |
| 你没见过的库名 | 可能编的 | "给我官方链接" |
| "清理了冗余代码" | 删了安全网 | "删了什么？逐条说" |
| 改了 5+ 文件每个 1-2 行 | 散弹枪 | "这是一件事吗？拆开做" |
| "增加了校验" (你要的是优化) | 偷换需求 | "用户体验变了吗？" |
| 代码出现旧方案元素 | 上下文污染 | 停对话 → 开新的 |
| "基于上一轮微调" | 偷跳步 | "是独立做的还是在上轮改的？" |
| 报告只有顶层结论 | 浅层执行 | "所有子项都查了吗？" |
| 做了 10 轮只对当前有效 | 目标漂移 | "对下个项目也有效吗？" |
| 同一文件多目录不一致 | 多副本 | "权威版本在哪？" |
| 又装了一个类似的库 | 重复依赖 | "已有依赖能做吗？" |
| 代码出现 sk-/密码/token | 密钥硬编码 | 立刻删 → 用环境变量 |

### 附录 E · 全周期检查清单

| 段 | 完成标志 | 没做的风险 |
|---|---|---|
| 1 启动 | KNOWLEDGE 已建 + git 已初始化 + rules 已建（安全/同步/纪律） | AI 反复失忆、密钥泄露、改动无法回退 |
| 初学者4关 | 知道先看 diff / 一次一件事 / 密钥用环境变量 / 人审安全代码 | 第一次对话就翻车 |
| 2 设计 | 模块拆分对齐 + 红线已列 + 验收标准已写 | 改一处崩三处 |
| 3 搭建 | 功能跑通 + KNOWLEDGE 已同步 + 代码已提交 | 进度丢失、无法回退 |
| 4 验证 | 每条有真实输出 + 红线人工试过 | 上线发现 AI 在"演" |
| 5 沉淀 | 换人能半小时接着干 | 经验留不下来 |

### 附录 F · 术语速查

| 术语 | 大白话 |
|---|---|
| KNOWLEDGE.md | 项目备忘录，AI 每次开工先读 |
| .cursor/rules/ | 给 AI 立的规矩，自动遵守 |
| 接口约定 | 模块的"门"——接受什么、输出什么 |
| 模块 | 项目拆成的一块块独立功能 |
| 红线 | 绝对不能出错的操作或数据 |
| git / commit | 存档，可回退到任意存档点 |
| Plan Mode | AI 只规划不写代码的模式 |
| `/compact` [CC] | 压缩对话历史恢复注意力 |
| Checkpoint [Cursor] | 自动存档点，可回退 |
| 上下文污染 | 旧方案讨论影响了 AI 对新方案的执行 |

---

## 收尾

> AI 不会让你不需要懂技术，但能把门槛从"会写代码"降到"会想清楚 + 会判断"。
>
> **你的护城河，是判断力，不是代码量。**
